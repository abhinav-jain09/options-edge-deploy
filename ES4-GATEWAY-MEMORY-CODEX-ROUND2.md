- HIGH — [k8s/es4/services/es-feed-gateway.yaml:196](/private/tmp/oe-deploy-mirror/k8s/es4/services/es-feed-gateway.yaml:196) — The `memory.peak`/`VmHWM` explanation is incorrect. `memory.peak` is a high-water mark and does not decrease when reclaim lowers current charged usage. File-backed resident pages are also ordinarily charged to the memory cgroup, so their inclusion in `VmHWM` does not explain `VmHWM > memory.peak`. The discrepancy instead indicates mismatched scope/lifetime, a reset, measurement timing, or accounting details that were not established. The same false explanation appears in the commit message. Using 649 MiB is conservative, but the asserted reason is not accurate. See the [kernel’s cgroup-v2 definition](https://www.kernel.org/doc/html/latest/admin-guide/cgroup-v2.html?highlight=memory.low).

- MEDIUM — [k8s/es4/services/es-feed-gateway.yaml:210](/private/tmp/oe-deploy-mirror/k8s/es4/services/es-feed-gateway.yaml:210) — One `memory.current` reading at 6h53m does not establish a 631 MiB “steady state” or prove that usage exceeded 512 MiB “at all times.” The eviction-ranking mechanism is otherwise correct: raising the request above observed usage improves ranking without changing Burstable QoS. State this as an observed point-in-time baseline, not an all-times fact.

- MEDIUM — [k8s/es4/services/es-feed-gateway.yaml:189](/private/tmp/oe-deploy-mirror/k8s/es4/services/es-feed-gateway.yaml:189) — “No heap tuning can prevent” the native-memory OOM is too absolute. Lowering `-Xmx` is heap tuning and would create native headroom under the existing limit. The correct claim is that GC tuning or reacting only to heap occupancy cannot prevent it while `Xmx == limit`. This overstatement is repeated in the commit message.

The resource changes themselves are coherent and independently safe: `2560Mi - 1536Mi = 1024Mi` satisfies G-R8; the 768 MiB request is schedulable against the recorded request headroom; Recreate removes overlap during rollout; and the init container does not increase the effective pod request. The relay remains disabled with no ES4 relay variables left half-applied. The node percentage arithmetic is internally consistent, though the live node observations are not independently reproducible from this repository.

VERDICT: REQUEST_CHANGES

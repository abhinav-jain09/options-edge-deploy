[Service]
# Kafka shares the production host with k3s. Prefer broker CPU and disk work during
# contention, protect its heap/native overhead from reclaim, and keep it out of swap.
CPUWeight=10000
IOWeight=10000
MemoryLow=14G
MemorySwapMax=0
OOMScoreAdjust=-500

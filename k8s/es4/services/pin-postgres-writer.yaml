# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/pin-postgres-writer/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: pin-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: pin-postgres-writer
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8098
    targetPort: health
  selector:
    app.kubernetes.io/name: pin-postgres-writer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: pin-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: pin-postgres-writer
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: pin-postgres-writer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pin-postgres-writer
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-pin-postgres-writer:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: pin-postgres-writer
        ports:
        - containerPort: 8098
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 1Gi
          requests:
            cpu: 100m
            memory: 256Mi

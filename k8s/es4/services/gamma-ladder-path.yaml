# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/gamma-ladder-path/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: gamma-ladder-path-service
    app.kubernetes.io/part-of: options-edge
  name: gamma-ladder-path-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8080
    targetPort: health
  selector:
    app.kubernetes.io/name: gamma-ladder-path-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: gamma-ladder-path-service
    app.kubernetes.io/part-of: options-edge
  name: gamma-ladder-path-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gamma-ladder-path-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gamma-ladder-path-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: KAFKA_LADDER_PATH_LADDER_TOPIC
          value: options.databento.gex.strike
        - name: KAFKA_LADDER_PATH_PREOPEN_TOPIC
          value: options.databento.gex.strike.preopen
        - name: KAFKA_LADDER_PATH_OI_STATUS_TOPIC
          value: options.databento.gex.oi-status
        - name: KAFKA_LADDER_PATH_SPOT_TOPIC
          value: underlying.spx.price
        - name: KAFKA_LADDER_PATH_ES_TOPIC
          value: underlying.es.price
        - name: KAFKA_LADDER_PATH_STATE_TOPIC
          value: options.spx.gamma-ladder-path.state
        - name: KAFKA_LADDER_PATH_EVENTS_TOPIC
          value: options.spx.gamma-ladder-path.events
        - name: LADDER_PATH_SYMBOL
          value: SPX
        - name: LADDER_PATH_SPOT_SYMBOL
          value: SPX
        - name: LADDER_PATH_ES_SYMBOL
          value: ES
        - name: LADDER_PATH_SPOT_SOURCE_FILTER
          value: ANY
        - name: LADDER_PATH_SPOT_FIELD_FILTER
          value: ANY
        - name: LADDER_PATH_HEARTBEAT_MS
          value: '60000'
        - name: LADDER_PATH_SPOT_FRESH_MS
          value: '60000'
        - name: LADDER_PATH_LADDER_FRESH_MS
          value: '120000'
        - name: LADDER_PATH_ES_FRESH_MS
          value: '60000'
        - name: LADDER_PATH_STATE_DIR
          value: /var/lib/options-edge/kafka-streams
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-gamma-ladder-path:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: gamma-ladder-path
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 1024Mi
          requests:
            cpu: 100m
            memory: 384Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state

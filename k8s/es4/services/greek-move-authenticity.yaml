# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/greek-move-authenticity/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: greek-move-authenticity-service
    app.kubernetes.io/name: greek-move-authenticity-service
    app.kubernetes.io/part-of: options-edge
  name: greek-move-authenticity-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8130
    targetPort: health
  selector:
    app.kubernetes.io/name: greek-move-authenticity-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: greek-move-authenticity-service
    app.kubernetes.io/name: greek-move-authenticity-service
    app.kubernetes.io/part-of: options-edge
  name: greek-move-authenticity-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: greek-move-authenticity-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: greek-move-authenticity-service
        app.kubernetes.io/name: greek-move-authenticity-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: GREEK_MOVE_AUTH_ENABLED
          value: 'true'
        - name: GREEK_MOVE_AUTH_BOOTSTRAP_IN_SESSION
          value: 'true'
        - name: HEALTH_PORT
          value: '8130'
        - name: JAVA_TOOL_OPTIONS
          value: -Xms256m -Xmx1024m
        - name: GREEK_MOVE_AUTH_SYMBOL
          value: ES
        - name: GREEK_MOVE_AUTH_INPUT_SPOT_TOPIC
          value: underlying.es.price
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-greek-move-authenticity:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: greek-move-authenticity
        ports:
        - containerPort: 8130
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 1Gi
          requests:
            cpu: 100m
            memory: 512Mi

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/volume-pace/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: volume-pace-databento-service
    app.kubernetes.io/part-of: options-edge
  name: volume-pace-databento-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: volume-pace-databento-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: volume-pace-databento-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: KAFKA_RAW_TOPIC
          value: options.databento.normalized
        - name: KAFKA_PACE_TOPIC
          value: options.databento.pace
        - name: KAFKA_PACE_STREAMS_APP_ID
          value: options-flow-pace-streams-databento
        - name: KAFKA_PACE_STREAMS_COMMIT_INTERVAL_MS
          value: '1000'
        - name: KAFKA_PACE_STREAMS_NUM_THREADS
          value: '2'
        - name: KAFKA_PACE_STREAMS_CACHE_MAX_BYTES_BUFFERING
          value: '1048576'
        - name: JAVA_TOOL_OPTIONS
          value: -Xms2g -Xmx6g
        - name: PACE_TRAJ_HORIZON_MS
          value: '24300000'
        - name: PACE_TRAJ_GRID_MS
          value: '300000'
        - name: PACE_TRAJ_MAX_FLOOR_AGE_MS
          value: '300000'
        - name: PACE_SESSION_CALENDAR
          value: es-globex
        - name: PACE_STALE_MS
          value: '120000'
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-volume-pace:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: volume-pace
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 9Gi
          requests:
            cpu: 250m
            memory: 2Gi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - name: kafka-streams-state
        persistentVolumeClaim:
          claimName: volume-pace-databento-service-streams-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: volume-pace-databento-service-streams-state
  namespace: options-edge
  labels:
    app.kubernetes.io/part-of: options-edge-es4
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

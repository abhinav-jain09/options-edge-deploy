# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/gamma-migration/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: gamma-migration-service
    app.kubernetes.io/part-of: options-edge
  name: gamma-migration-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8080
    targetPort: health
  selector:
    app.kubernetes.io/name: gamma-migration-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: gamma-migration-service
    app.kubernetes.io/part-of: options-edge
  name: gamma-migration-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gamma-migration-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: gamma-migration-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: KAFKA_GAMMA_MIGRATION_INPUT_TOPIC
          value: options.databento.gex.strike
        - name: KAFKA_GAMMA_MIGRATION_OUTPUT_TOPIC
          value: options.spx.gamma-migration.current
        - name: KAFKA_GAMMA_MIGRATION_SCORING_TOPIC
          value: options.spx.gamma-migration.scoring
        - name: GAMMA_MIGRATION_EMIT_INTERVAL_MS
          value: '1000'
        - name: GAMMA_MIGRATION_PARKED_DWELL_MS
          value: '600000'
        - name: GAMMA_MIGRATION_MIN_PTS
          value: '7.5'
        - name: GAMMA_MIGRATION_WINDOW_MS
          value: '900000'
        - name: GAMMA_MIGRATION_ROTATION_MIN_SKEW_RATE
          value: '0.012'
        - name: GAMMA_MIGRATION_ATM_EPSILON_PTS
          value: '2.5'
        - name: GAMMA_MIGRATION_STALE_STRIKE_MS
          value: '300000'
        - name: GAMMA_MIGRATION_LADDER_INTERVAL_MS
          value: '15000'
        - name: GAMMA_MIGRATION_MIN_STRIKES
          value: '8'
        - name: GAMMA_MIGRATION_JUDGE_FLIP_MIN_FRACTION
          value: '0.05'
        - name: GAMMA_MIGRATION_COMPLETE_MIN_FRACTION
          value: '0.55'
        - name: GAMMA_MIGRATION_WIDTH_DECAY_MS
          value: '1800000'
        - name: GAMMA_MIGRATION_CHAIN_IDLE_MS
          value: '2700000'
        - name: GAMMA_MIGRATION_SESSION_SAVE_INTERVAL_MS
          value: '30000'
        - name: KAFKA_GAMMA_MIGRATION_TOPIC_PARTITIONS
          value: '4'
        - name: KAFKA_GAMMA_MIGRATION_NUM_THREADS
          value: '1'
        - name: KAFKA_GAMMA_MIGRATION_OTHER_BOARD_BOOTSTRAP
          value: 192.168.100.252:9092
        - name: KAFKA_GAMMA_MIGRATION_OTHER_BOARD_TOPIC
          value: options.databento.gex.strike.history
        - name: KAFKA_GAMMA_MIGRATION_OTHER_BOARD_SYMBOL
          value: SPX
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-gamma-migration:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: gamma-migration
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 1536Mi
          requests:
            cpu: 100m
            memory: 512Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state

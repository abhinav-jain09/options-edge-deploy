# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/strike-liquidity-heatmap/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: strike-liquidity-heatmap-service
    app.kubernetes.io/name: strike-liquidity-heatmap-service
    app.kubernetes.io/part-of: options-edge
  name: strike-liquidity-heatmap-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8111
    targetPort: health
  selector:
    app.kubernetes.io/name: strike-liquidity-heatmap-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: strike-liquidity-heatmap-service
    app.kubernetes.io/name: strike-liquidity-heatmap-service
    app.kubernetes.io/part-of: options-edge
  name: strike-liquidity-heatmap-service
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: strike-liquidity-heatmap-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: strike-liquidity-heatmap-service
        app.kubernetes.io/name: strike-liquidity-heatmap-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: HEALTH_PORT
          value: '8111'
        - name: STRIKE_LIQUIDITY_INPUT_EVENTS_TOPIC
          value: options.databento.events.raw
        - name: STRIKE_LIQUIDITY_INPUT_STRIKE_FLOW_TOPIC
          value: options.databento.strike-flow
        - name: STRIKE_LIQUIDITY_STREAMS_APP_ID
          value: options-edge-strike-liquidity-heatmap-v1
        - name: STRIKE_LIQUIDITY_STREAMS_COMMIT_INTERVAL_MS
          value: '1000'
        - name: STRIKE_LIQUIDITY_STREAMS_AUTO_OFFSET_RESET
          value: latest
        - name: STRIKE_LIQUIDITY_STREAMS_NUM_THREADS
          value: '1'
        - name: KAFKA_STREAMS_STATE_DIR
          value: /var/lib/options-edge/kafka-streams
        - name: JAVA_TOOL_OPTIONS
          value: -Xms256m -Xmx768m
        envFrom:
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-strike-liquidity-heatmap:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: strike-liquidity-heatmap
        ports:
        - containerPort: 8111
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 2Gi
          requests:
            cpu: 100m
            memory: 512Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - name: kafka-streams-state
        persistentVolumeClaim:
          claimName: strike-liquidity-heatmap-service-streams-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: strike-liquidity-heatmap-service-streams-state
  namespace: options-edge
  labels:
    app.kubernetes.io/part-of: options-edge-es4
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/databento-volume-aggregator/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8080'
    prometheus.io/scrape: 'true'
  labels:
    app.kubernetes.io/name: databento-volume-aggregator
    app.kubernetes.io/part-of: options-edge
  name: databento-volume-aggregator
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8080
    targetPort: health
  selector:
    app.kubernetes.io/name: databento-volume-aggregator
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: databento-volume-aggregator
    app.kubernetes.io/part-of: options-edge
    oe.replay/determinism: event-time
    oe.replay/include: 'true'
    oe.replay/side-effects: 'false'
    oe.replay/state: ephemeral
    oe.replay/topic-remap: required
  name: databento-volume-aggregator
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-volume-aggregator
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databento-volume-aggregator
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: KAFKA_DATABENTO_EVENTS_TOPIC
          value: options.databento.events.raw
        - name: KAFKA_DATABENTO_SNAPSHOT_TOPIC
          value: options.databento.raw
        - name: KAFKA_DATABENTO_NORMALIZED_TOPIC
          value: options.databento.normalized
        - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_APP_ID
          value: options-flow-databento-volume-aggregator
        - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_STREAMS_NUM_THREADS
          value: '2'
        - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_CACHE_MAX_BYTES_BUFFERING
          value: '0'
        - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_PROCESSING_GUARANTEE
          value: exactly_once_v2
        - name: JAVA_TOOL_OPTIONS
          value: -Xmx1536m
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-databento-volume-aggregator:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: databento-volume-aggregator
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 3Gi
          requests:
            cpu: 250m
            memory: 512Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - name: kafka-streams-state
        persistentVolumeClaim:
          claimName: databento-volume-aggregator-streams-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: databento-volume-aggregator-streams-state
  namespace: options-edge
  labels:
    app.kubernetes.io/part-of: options-edge-es4
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/strike-intelligence/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: strike-intelligence-service
    app.kubernetes.io/name: strike-intelligence-service
    app.kubernetes.io/part-of: options-edge
  name: strike-intelligence-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8114
    targetPort: health
  selector:
    app.kubernetes.io/name: strike-intelligence-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: strike-intelligence-service
    app.kubernetes.io/name: strike-intelligence-service
    app.kubernetes.io/part-of: options-edge
  name: strike-intelligence-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: strike-intelligence-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: strike-intelligence-service
        app.kubernetes.io/name: strike-intelligence-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: HEALTH_PORT
          value: '8114'
        - name: STRIKE_INTEL_INPUT_STRIKE_FLOW_TOPIC
          value: options.databento.strike-flow
        - name: STRIKE_INTEL_INPUT_DELTA_FLOW_TOPIC
          value: delta-flow-by-strike
        - name: STRIKE_INTEL_INPUT_PRICE_BEHAVIOR_TOPIC
          value: option-price-behavior-by-strike
        - name: STRIKE_INTEL_INPUT_GEX_FLOW_TOPIC
          value: options.databento.gex.flow.by-strike
        - name: STRIKE_INTEL_INPUT_UNDERLYING_TOPIC
          value: underlying.es.price
        - name: STRIKE_INTEL_BY_STRIKE_TOPIC
          value: strike-intelligence-by-strike
        - name: STRIKE_INTEL_DASHBOARD_TOPIC
          value: strike-intelligence-dashboard
        - name: STRIKE_INTEL_STREAMS_APP_ID
          value: options-edge-strike-intelligence-service
        - name: STRIKE_INTEL_STREAMS_COMMIT_INTERVAL_MS
          value: '1000'
        - name: STRIKE_INTEL_STREAMS_AUTO_OFFSET_RESET
          value: latest
        - name: STRIKE_INTEL_STREAMS_NUM_THREADS
          value: '1'
        - name: STRIKE_INTEL_EOS_ENABLED
          value: 'true'
        - name: KAFKA_STREAMS_STATE_DIR
          value: /var/lib/options-edge/kafka-streams
        - name: STRIKE_INTEL_ENABLED
          value: 'true'
        - name: STRIKE_INTEL_ACTION_BIAS_ENABLED
          value: 'true'
        - name: JAVA_TOOL_OPTIONS
          value: -Xms512m -Xmx2048m
        - name: STRIKE_INTEL_ADAPTIVE_SCALE_ENABLED
          value: 'true'
        # §7c premium floor (processing #371): the adaptive scaler rates the session's top slice
        # at volumeScore 1.0 regardless of ABSOLUTE size — on thin Globex tape a $46k far-OTM
        # trickle emitted BUY_CALL (2026-07-17 overnight, 7630). Below $100k the action gates to
        # NONE ("below premium floor"); roles/dashboard stay informative. Tune from trail data:
        # observed noise $31-46k, plausible signals $112k+.
        - name: STRIKE_INTEL_ACTION_MIN_TOTAL_PREMIUM
          value: '100000'
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-strike-intelligence:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: strike-intelligence
        ports:
        - containerPort: 8114
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 4Gi
          requests:
            cpu: 100m
            memory: 1Gi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state

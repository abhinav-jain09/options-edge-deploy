# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/delta-flow/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: delta-flow-service
    app.kubernetes.io/name: delta-flow-service
    app.kubernetes.io/part-of: options-edge
  name: delta-flow-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8110
    targetPort: health
  selector:
    app.kubernetes.io/name: delta-flow-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: delta-flow-service
    app.kubernetes.io/name: delta-flow-service
    app.kubernetes.io/part-of: options-edge
  name: delta-flow-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: delta-flow-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: delta-flow-service
        app.kubernetes.io/name: delta-flow-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: HEALTH_PORT
          value: '8110'
        - name: DELTA_FLOW_INPUT_TCBBO_TOPIC
          value: options.opra.tcbbo
        - name: DELTA_FLOW_INPUT_GEX_TOPIC
          value: options.databento.gex.strike
        - name: DELTA_FLOW_STREAMS_APP_ID
          value: options-edge-delta-flow-service
        - name: DELTA_FLOW_STREAMS_COMMIT_INTERVAL_MS
          value: '1000'
        - name: DELTA_FLOW_STREAMS_AUTO_OFFSET_RESET
          value: latest
        - name: DELTA_FLOW_STREAMS_NUM_THREADS
          value: '1'
        - name: KAFKA_STREAMS_STATE_DIR
          value: /var/lib/options-edge/kafka-streams
        - name: JAVA_TOOL_OPTIONS
          value: -Xms256m -Xmx768m
        - name: DELTA_FLOW_DEFAULT_CONTRACT_MULTIPLIER
          value: '50'
        - name: DELTA_FLOW_VERIFIED_MULTIPLIERS
          value: ES:50
        # ES trades on GLBX/CME, which stamps the TRUE aggressor side (trades.side B/A). Use it instead of
        # trade-vs-NBBO estimation. Gated additionally by GLBX-dataset provenance in-code, so this is inert
        # for any non-GLBX data. SPX/OPRA (side='N') is untouched and keeps this flag OFF (its default).
        - name: DELTA_FLOW_USE_NATIVE_SIDE
          value: 'true'
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-delta-flow:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: delta-flow
        ports:
        - containerPort: 8110
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 4Gi
          requests:
            cpu: 100m
            memory: 384Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/databento-gex/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :dev tag before apply).
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8100'
    prometheus.io/scrape: 'true'
  labels:
    app.kubernetes.io/name: databento-gex-service
    app.kubernetes.io/part-of: options-edge
  name: databento-gex-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8100
    targetPort: health
  selector:
    app.kubernetes.io/name: databento-gex-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: databento-gex-service
    app.kubernetes.io/part-of: options-edge
  name: databento-gex-service
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-gex-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databento-gex-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: DATABENTO_DATASET
          valueFrom:
            configMapKeyRef:
              key: DATABENTO_DATASET
              name: options-edge-databento-feed-config
              optional: true
        - name: DATABENTO_GEX_OI_TRADING_CLASS
          valueFrom:
            configMapKeyRef:
              key: DATABENTO_TRADING_CLASS
              name: options-edge-databento-feed-config
              optional: true
        - name: DATABENTO_GEX_OI_HTTP_TIMEOUT_SECONDS
          value: '60'
        - name: DATABENTO_GEX_OI_FETCH_LIMIT
          value: '200000'
        - name: HEALTH_PORT
          value: '8100'
        - name: KAFKA_DATABENTO_RAW_TOPIC
          value: options.databento.raw
        - name: KAFKA_DATABENTO_GEX_TOPIC
          value: options.databento.gex.strike
        - name: KAFKA_GEX_STREAMS_APP_ID
          value: options-databento-gex-streams-v1
        - name: KAFKA_GEX_STREAMS_NUM_THREADS
          value: '8'
        - name: DATABENTO_GEX_DEALER_SIGN_MODEL
          value: NAIVE_CALL_PLUS_PUT_MINUS
        - name: DATABENTO_GEX_OI_DIRECT_FETCH_ENABLED
          value: 'true'
        - name: DATABENTO_GEX_MAX_QUOTE_AGE_MS
          value: '600000'
        - name: JAVA_TOOL_OPTIONS
          value: -Xms1g -Xmx4g
        - name: DATABENTO_GEX_RISK_FREE_RATE
          value: '0.04'
        - name: DATABENTO_GEX_DIVIDEND_YIELD
          value: '0.04'
        - name: DATABENTO_GEX_OI_BASELINE_BACKFILL_ENABLED
          value: 'false'
        envFrom:
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-databento-gex:dev
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: databento-gex
        ports:
        - containerPort: 8100
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 5Gi
          requests:
            cpu: 250m
            memory: 1Gi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - name: kafka-streams-state
        persistentVolumeClaim:
          claimName: databento-gex-service-streams-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: databento-gex-service-streams-state
  namespace: options-edge
  labels:
    app.kubernetes.io/part-of: options-edge-es4
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/dealer-ledger-calibration/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: dealer-ledger-calibration-scorer
    app.kubernetes.io/name: dealer-ledger-calibration-scorer
    app.kubernetes.io/part-of: options-edge
  name: dealer-ledger-calibration-scorer
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dealer-ledger-calibration-scorer
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: dealer-ledger-calibration-scorer
        app.kubernetes.io/name: dealer-ledger-calibration-scorer
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: CALIBRATION_ROLE
          value: scorer
        - name: HEALTH_PORT
          value: '8080'
        - name: SCORER_INPUT_SIGNAL_FIRED_TOPIC
          value: dealer-ledger-signal-fired
        - name: SCORER_INPUT_SPOT_TOPIC
          value: underlying.spx.price
        - name: SCORER_OUTPUT_OUTCOME_SCORED_TOPIC
          value: dealer-ledger-outcome-scored
        - name: KAFKA_STREAMS_STATE_DIR
          value: /var/lib/options-edge/kafka-streams
        - name: SCORER_BAR_SIZE_MS
          value: '60000'
        - name: SCORER_SESSION_GRACE_MS
          value: '120000'
        - name: JAVA_TOOL_OPTIONS
          value: -Xms512m -Xmx2560m
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-dealer-ledger-calibration:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 15
        name: dealer-ledger-calibration-scorer
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 4Gi
          requests:
            cpu: 100m
            memory: 1Gi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: dealer-ledger-calibration-accumulator
    app.kubernetes.io/name: dealer-ledger-calibration-accumulator
    app.kubernetes.io/part-of: options-edge
  name: dealer-ledger-calibration-accumulator
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dealer-ledger-calibration-accumulator
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: dealer-ledger-calibration-accumulator
        app.kubernetes.io/name: dealer-ledger-calibration-accumulator
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: CALIBRATION_ROLE
          value: accumulator
        - name: HEALTH_PORT
          value: '8080'
        - name: CALIBRATION_OUTCOME_SCORED_TOPIC
          value: dealer-ledger-outcome-scored
        - name: CALIBRATION_STATE_TOPIC
          value: dealer-ledger-calibration-state
        - name: CALIBRATION_PRIMARY_HORIZON
          value: M15
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-dealer-ledger-calibration:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 15
        name: dealer-ledger-calibration-accumulator
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 1024Mi
          requests:
            cpu: 100m
            memory: 384Mi

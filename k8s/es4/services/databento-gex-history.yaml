# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/databento-gex-history/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: databento-gex-history-service
    app.kubernetes.io/part-of: options-edge
  name: databento-gex-history-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8080
    targetPort: health
  selector:
    app.kubernetes.io/name: databento-gex-history-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: databento-gex-history-service
    app.kubernetes.io/part-of: options-edge
  name: databento-gex-history-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-gex-history-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databento-gex-history-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: KAFKA_DATABENTO_GEX_TOPIC
          value: options.databento.gex.strike
        - name: KAFKA_DATABENTO_GEX_HISTORY_TOPIC
          value: options.databento.gex.strike.history
        - name: KAFKA_DATABENTO_GEX_HISTORY_STREAMS_APP_ID
          value: options-databento-gex-history-streams-v1
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-databento-gex-history:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: databento-gex-history
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 2Gi
          requests:
            cpu: 100m
            memory: 384Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - name: kafka-streams-state
        persistentVolumeClaim:
          claimName: databento-gex-history-service-streams-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: databento-gex-history-service-streams-state
  namespace: options-edge
  labels:
    app.kubernetes.io/part-of: options-edge-es4
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/tape-zones/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: tape-zones-service
    app.kubernetes.io/part-of: options-edge
  name: tape-zones-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8140
    targetPort: health
  selector:
    app.kubernetes.io/name: tape-zones-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: tape-zones-service
    app.kubernetes.io/part-of: options-edge
  name: tape-zones-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: tape-zones-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tape-zones-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: TAPE_ZONES_BOOTSTRAP_SERVERS
          value: 192.168.100.4:9092
        - name: KAFKA_ES_TRADES_TOPIC
          value: es.underlying.es.trades
        - name: KAFKA_TAPE_ZONES_CELLS_TOPIC
          value: es.tape-zones.cells
        - name: KAFKA_TAPE_ZONES_BOARD_TOPIC
          value: es.tape-zones.board
        - name: HEALTH_PORT
          value: '8140'
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-tape-zones:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: tape-zones
        ports:
        - containerPort: 8140
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            cpu: '1'
            memory: 2Gi
          requests:
            cpu: 250m
            memory: 512Mi

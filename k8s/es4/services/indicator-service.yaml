# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/indicator-service/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8133'
    prometheus.io/scrape: 'true'
  labels:
    app.kubernetes.io/component: indicator-service
    app.kubernetes.io/name: indicator-service
    app.kubernetes.io/part-of: options-edge
  name: indicator-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8133
    targetPort: health
  selector:
    app.kubernetes.io/name: indicator-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: indicator-service
    app.kubernetes.io/name: indicator-service
    app.kubernetes.io/part-of: options-edge
  name: indicator-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: indicator-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: indicator-service
        app.kubernetes.io/name: indicator-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: HEALTH_PORT
          value: '8133'
        - name: INDICATOR_SOURCE_MODE
          value: ES_TRADES
        - name: LIVENESS_SESSION
          value: globex
        - name: INDICATOR_INPUT_PARTITIONS
          value: '4'
        - name: INDICATOR_COLD_BOOTSTRAP
          value: 'true'
        - name: INDICATOR_EPOCH_GENESIS
          value: 'false'
        # TOPIC_PREFIX=es. (es4-common-env) prefixes the control-topic default at
        # runtime — an explicit value here would double-prefix.
        - name: INDICATOR_SESSION_BACKFILL
          value: 'true'
        - name: INDICATOR_STATE_DIR
          value: /home/options-edge/indicator-service/state
        - name: JAVA_TOOL_OPTIONS
          value: -Xms256m -Xmx1536m
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-indicator-service:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: indicator-service
        ports:
        - containerPort: 8133
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 2Gi
          requests:
            cpu: 200m
            memory: 512Mi
        volumeMounts:
        - mountPath: /home/options-edge/indicator-service/state
          name: state
      volumes:
      - emptyDir:
          sizeLimit: 5Gi
        name: state

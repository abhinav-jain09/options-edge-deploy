# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/strike-flow-avro-adapter/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8109'
    prometheus.io/scrape: 'true'
  labels:
    app.kubernetes.io/name: strike-flow-avro-adapter
    app.kubernetes.io/part-of: options-edge
  name: strike-flow-avro-adapter
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8109
    targetPort: health
  selector:
    app.kubernetes.io/name: strike-flow-avro-adapter
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: strike-flow-avro-adapter
    app.kubernetes.io/part-of: options-edge
  name: strike-flow-avro-adapter
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: strike-flow-avro-adapter
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: strike-flow-avro-adapter
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: HEALTH_PORT
          value: '8109'
        - name: KAFKA_STRIKE_FLOW_AVRO_INPUT_TOPIC
          value: options.databento.strike-flow
        - name: KAFKA_STRIKE_FLOW_AVRO_OUTPUT_TOPIC
          value: options.databento.strike-flow.strike.avro
        - name: KAFKA_STRIKE_FLOW_AVRO_STREAMS_APP_ID
          value: options-edge-strike-flow-avro-adapter
        - name: KAFKA_STRIKE_FLOW_AVRO_STREAMS_NUM_THREADS
          value: '8'
        - name: JAVA_TOOL_OPTIONS
          value: -Xms256m -Xmx768m
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-strike-flow-avro-adapter:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: strike-flow-avro-adapter
        ports:
        - containerPort: 8109
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 4Gi
          requests:
            cpu: 100m
            memory: 384Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/reversal-postgres-writer/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: reversal-postgres-writer
    app.kubernetes.io/name: reversal-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: reversal-postgres-writer
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8080
    targetPort: health
  selector:
    app.kubernetes.io/name: reversal-postgres-writer
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: reversal-postgres-writer
    app.kubernetes.io/name: reversal-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: reversal-postgres-writer
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: reversal-postgres-writer
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: reversal-postgres-writer
        app.kubernetes.io/name: reversal-postgres-writer
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: HEALTH_PORT
          value: '8080'
        - name: REVERSAL_WRITER_BOOTSTRAP_SERVERS
          value: 192.168.100.4:9092
        - name: KAFKA_REVERSAL_FINAL_SUMMARY_TOPIC
          value: es.reversal.final-summary
        - name: KAFKA_REVERSAL_OUTCOME_TOPIC
          value: es.reversal.outcome
        - name: KAFKA_REVERSAL_VERDICTS_TOPIC
          value: es.reversal.verdicts
        - name: JAVA_TOOL_OPTIONS
          value: -Xms128m -Xmx512m
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-reversal-postgres-writer:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: reversal-postgres-writer
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 768Mi
          requests:
            cpu: 50m
            memory: 256Mi

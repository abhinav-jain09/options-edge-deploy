# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/spread-skew/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: spread-skew-service
    app.kubernetes.io/name: spread-skew-service
    app.kubernetes.io/part-of: options-edge
  name: spread-skew-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8122
    targetPort: health
  selector:
    app.kubernetes.io/name: spread-skew-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: spread-skew-service
    app.kubernetes.io/name: spread-skew-service
    app.kubernetes.io/part-of: options-edge
  name: spread-skew-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: spread-skew-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: spread-skew-service
        app.kubernetes.io/name: spread-skew-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: HEALTH_PORT
          value: '8122'
        - name: SPREAD_SKEW_INPUT_QUOTE_TOPIC
          value: options.databento.events.raw
        - name: SPREAD_SKEW_INPUT_SPOT_TOPIC
          value: underlying.es.trades
        - name: SPREAD_SKEW_OUTPUT_CURRENT_TOPIC
          value: options.es.spread-skew.current
        - name: SPREAD_SKEW_OUTPUT_EVENTS_TOPIC
          value: options.es.spread-skew.events
        - name: SPREAD_SKEW_CONSUMER_GROUP
          value: spread-skew-service
        - name: JAVA_TOOL_OPTIONS
          value: -Xms512m -Xmx2048m
        - name: SPREAD_SKEW_UNDERLYING
          value: ES
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-spread-skew:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: spread-skew
        ports:
        - containerPort: 8122
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 4Gi
          requests:
            cpu: 100m
            memory: 1Gi

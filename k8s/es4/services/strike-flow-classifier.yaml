# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/strike-flow-classifier/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: strike-flow-classifier
    app.kubernetes.io/name: strike-flow-classifier-databento
    app.kubernetes.io/part-of: options-edge
  name: strike-flow-classifier-databento
  namespace: options-edge
spec:
  ports:
  - name: http
    port: 8097
    targetPort: http
  selector:
    app.kubernetes.io/name: strike-flow-classifier-databento
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: strike-flow-classifier
    app.kubernetes.io/name: strike-flow-classifier-databento
    app.kubernetes.io/part-of: options-edge
    oe.replay/determinism: event-time
    oe.replay/include: 'true'
    oe.replay/side-effects: 'false'
    oe.replay/state: ephemeral
    oe.replay/topic-remap: required
  name: strike-flow-classifier-databento
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: strike-flow-classifier-databento
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: strike-flow-classifier
        app.kubernetes.io/name: strike-flow-classifier-databento
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: STRIKE_FLOW_SOURCE
          value: DATABENTO
        - name: STRIKE_FLOW_ENABLED
          value: 'true'
        - name: STRIKE_FLOW_DELTA_OVERLAY_ENABLED
          value: 'false'
        - name: KAFKA_STREAMS_CLEANUP_ON_START
          value: 'true'
        - name: STRIKE_FLOW_DELTA_REDIS_HOST
          value: 192.168.100.4
        - name: STRIKE_FLOW_DELTA_REDIS_PORT
          value: '6379'
        - name: STRIKE_FLOW_DELTA_REDIS_TIMEOUT_MS
          value: '50'
        - name: STRIKE_FLOW_DELTA_REDIS_KEY_PREFIX
          value: 'gexdelta:'
        - name: STRIKE_FLOW_INSTITUTIONAL_ENABLED
          value: 'true'
        - name: STRIKE_FLOW_INSTITUTIONAL_TICK_EPSILON
          value: '0.0'
        - name: STRIKE_FLOW_INSTITUTIONAL_FRESHNESS_MS
          value: '3000'
        - name: STRIKE_FLOW_WINDOWS_ENABLED
          value: 'true'
        - name: STRIKE_FLOW_SWEEP_ENABLED
          value: 'true'
        - name: STRIKE_FLOW_QUALITY_ENABLED
          value: 'true'
        - name: KAFKA_STRIKE_FLOW_INPUT_TOPIC
          value: options.opra.tcbbo
        - name: KAFKA_STRIKE_FLOW_OUTPUT_TOPIC
          value: options.databento.strike-flow
        - name: KAFKA_SELLER_ACTIVITY_TOPIC
          value: options.databento.seller-activity
        - name: KAFKA_STRIKE_FLOW_STREAMS_APP_ID
          value: options-edge-databento-strike-flow-classifier
        - name: KAFKA_STRIKE_FLOW_STREAMS_AUTO_OFFSET_RESET
          value: earliest
        - name: KAFKA_STRIKE_FLOW_STREAMS_NUM_THREADS
          value: '8'
        # Authoritative instrument allow-list since processing PR#494: the classifier derives each trade's
        # symbol from the payload's `underlying` field, which is "ES" for every GLBX ES-option record on
        # es.options.opra.tcbbo (weekly roots E1A..E4E/EW arrive as `product`, NOT as the symbol). The stale
        # "SPX" here predated the es4 ES-ification (multiplier 50 below) and, once the allow-list became
        # authoritative, rejected 100% of es4 input (rejected_symbol_total 1073+, processed 0, 2026-07-27).
        - name: STRIKE_FLOW_SYMBOL
          value: ES
        - name: STRIKE_FLOW_OUTPUT_INTERVAL_MS
          value: '500'
        - name: STRIKE_FLOW_BIAS_RATIO
          value: '1.20'
        - name: STRIKE_FLOW_CONTRACT_MULTIPLIER
          value: '50'
        - name: HEALTH_PORT
          value: '8097'
        - name: JAVA_TOOL_OPTIONS
          value: -Xms2g -Xmx4g
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-strike-flow-classifier:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 30
          periodSeconds: 15
        name: strike-flow-classifier
        ports:
        - containerPort: 8097
          name: http
        readinessProbe:
          httpGet:
            path: /health/ready
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 8Gi
          requests:
            cpu: '1'
            memory: 3Gi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - name: kafka-streams-state
        persistentVolumeClaim:
          claimName: strike-flow-classifier-databento-streams-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: strike-flow-classifier-databento-streams-state
  namespace: options-edge
  labels:
    app.kubernetes.io/part-of: options-edge-es4
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/context-tape/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8135'
    prometheus.io/scrape: 'true'
  labels:
    app.kubernetes.io/component: context-tape-service
    app.kubernetes.io/name: context-tape-service
    app.kubernetes.io/part-of: options-edge
  name: context-tape-service
  namespace: options-edge
spec:
  ports:
  - name: api
    port: 8134
    targetPort: api
  - name: health
    port: 8135
    targetPort: health
  selector:
    app.kubernetes.io/name: context-tape-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: context-tape-service
    app.kubernetes.io/name: context-tape-service
    app.kubernetes.io/part-of: options-edge
  name: context-tape-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: context-tape-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: context-tape-service
        app.kubernetes.io/name: context-tape-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: HEALTH_PORT
          value: '8135'
        - name: CONTEXT_TAPE_SPOT_TOPIC
          value: underlying.spx.price
        - name: CONTEXT_TAPE_GEX_HISTORY_TOPIC
          value: options.databento.gex.strike.history
        - name: CONTEXT_TAPE_ES_TRADES_TOPIC
          value: underlying.es.trades
        - name: JAVA_TOOL_OPTIONS
          value: -Xms256m -Xmx768m
        - name: CONTEXT_TAPE_SPOT_SOURCE_FILTER
          value: SYNTHETIC_OPTION_SPOT
        - name: CONTEXT_TAPE_SPOT_FIELD_FILTER
          value: ANY
        - name: CONTEXT_TAPE_GEX_SYMBOL_FILTER
          value: ES
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-context-tape:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: context-tape
        ports:
        - containerPort: 8134
          name: api
        - containerPort: 8135
          name: health
        readinessProbe:
          failureThreshold: 30
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 1Gi
          requests:
            cpu: 250m
            memory: 512Mi

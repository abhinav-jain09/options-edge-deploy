# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/directional-pressure/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: directional-pressure-databento-service
    app.kubernetes.io/part-of: options-edge
    oe.replay/determinism: event-time
    oe.replay/include: 'true'
    oe.replay/side-effects: 'false'
    oe.replay/state: ephemeral
    oe.replay/topic-remap: required
  name: directional-pressure-databento-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: directional-pressure-databento-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: directional-pressure-databento-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: KAFKA_RAW_TOPIC
          value: options.databento.normalized
        - name: KAFKA_DIRECTIONAL_PRESSURE_TOPIC
          value: options.databento.directional-pressure
        - name: KAFKA_DIRECTIONAL_PRESSURE_STREAMS_APP_ID
          value: options-flow-directional-pressure-streams-v4-databento
        - name: KAFKA_DIRECTIONAL_PRESSURE_STREAMS_COMMIT_INTERVAL_MS
          value: '1000'
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-directional-pressure:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: directional-pressure
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 2560Mi
          requests:
            cpu: 250m
            memory: 512Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - name: kafka-streams-state
        persistentVolumeClaim:
          claimName: directional-pressure-databento-service-streams-state
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: directional-pressure-databento-service-streams-state
  namespace: options-edge
  labels:
    app.kubernetes.io/part-of: options-edge-es4
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

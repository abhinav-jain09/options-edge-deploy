# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/es-open-direction-postgres-writer/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: es-open-direction-postgres-writer
    app.kubernetes.io/name: es-open-direction-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: es-open-direction-postgres-writer
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8080
    targetPort: health
  selector:
    app.kubernetes.io/name: es-open-direction-postgres-writer
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: es-open-direction-postgres-writer
    app.kubernetes.io/name: es-open-direction-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: es-open-direction-postgres-writer
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: es-open-direction-postgres-writer
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: es-open-direction-postgres-writer
        app.kubernetes.io/name: es-open-direction-postgres-writer
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: HEALTH_PORT
          value: '8080'
        - name: KAFKA_ES_OPEN_DIRECTION_FORECAST_TOPIC
          value: es.open-direction.forecast
        - name: KAFKA_ES_OPEN_DIRECTION_OUTCOME_TOPIC
          value: es.open-direction.outcome
        - name: JAVA_TOOL_OPTIONS
          value: -Xms128m -Xmx512m
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-es-open-direction-postgres-writer:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: es-open-direction-postgres-writer
        ports:
        - containerPort: 8080
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 768Mi
          requests:
            cpu: 50m
            memory: 256Mi

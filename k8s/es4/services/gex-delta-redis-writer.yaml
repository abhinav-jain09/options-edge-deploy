# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/gex-delta-redis-writer/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :dev tag before apply).
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8101'
    prometheus.io/scrape: 'true'
  labels:
    app.kubernetes.io/name: gex-delta-redis-writer
    app.kubernetes.io/part-of: options-edge
  name: gex-delta-redis-writer
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8101
    targetPort: health
  selector:
    app.kubernetes.io/name: gex-delta-redis-writer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: gex-delta-redis-writer
    app.kubernetes.io/name: gex-delta-redis-writer
    app.kubernetes.io/part-of: options-edge
  name: gex-delta-redis-writer
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: gex-delta-redis-writer
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: gex-delta-redis-writer
        app.kubernetes.io/name: gex-delta-redis-writer
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: HEALTH_PORT
          value: '8101'
        - name: KAFKA_DATABENTO_GEX_STRIKE_TOPIC
          value: options.databento.gex.strike
        - name: GEX_DELTA_REDIS_HOST
          value: 192.168.100.4
        - name: GEX_DELTA_REDIS_PORT
          value: '6379'
        - name: GEX_DELTA_REDIS_KEY_PREFIX
          value: 'gexdelta:'
        - name: GEX_DELTA_REDIS_TTL_MS
          value: '300000'
        - name: JAVA_TOOL_OPTIONS
          value: -Xmx512m
        envFrom:
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-gex-delta-redis-writer:dev
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: gex-delta-redis-writer
        ports:
        - containerPort: 8101
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 4Gi
          requests:
            cpu: 100m
            memory: 256Mi

# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/option-truth-engine/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: option-truth-engine-service
    app.kubernetes.io/name: option-truth-engine-service
    app.kubernetes.io/part-of: options-edge
  name: option-truth-engine-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8131
    targetPort: health
  selector:
    app.kubernetes.io/name: option-truth-engine-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: option-truth-engine-service
    app.kubernetes.io/name: option-truth-engine-service
    app.kubernetes.io/part-of: options-edge
  name: option-truth-engine-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: option-truth-engine-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: option-truth-engine-service
        app.kubernetes.io/name: option-truth-engine-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: HEALTH_PORT
          value: '8131'
        - name: OPTION_TRUTH_ENABLED
          value: 'true'
        - name: OPTION_TRUTH_INPUT_TOPIC
          value: options.databento.gex.strike
        - name: OPTION_TRUTH_OUTPUT_TOPIC
          value: options.es.option-truth-engine-service.by-strike
        - name: OPTION_TRUTH_STREAMS_APP_ID
          value: options-edge-option-truth-engine-service
        - name: OPTION_TRUTH_PRICING_MODEL
          value: FUTURES_BLACK_76
        - name: OPTION_TRUTH_EXCLUDE_ZERO_BID
          value: 'true'
        - name: OPTION_TRUTH_EOS_ENABLED
          value: 'true'
        - name: KAFKA_STREAMS_STATE_DIR
          value: /var/lib/options-edge/kafka-streams
        - name: JAVA_TOOL_OPTIONS
          value: -Xms256m -Xmx768m
        envFrom:
        - configMapRef:
            name: es4-base-env
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-option-truth-engine:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: option-truth-engine
        ports:
        - containerPort: 8131
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 30
          periodSeconds: 10
        resources:
          limits:
            memory: 1Gi
          requests:
            cpu: 100m
            memory: 512Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state

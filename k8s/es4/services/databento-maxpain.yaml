# GENERATED by scripts/es4/render_es4_manifests.py from k8s/services/databento-maxpain/overlays/production/manifest.yaml — DO NOT EDIT BY HAND.
# es4 environment: env-level knobs come from es4-common-env / es4-runtime-secrets;
# every topic gets the es. prefix at runtime via TOPIC_PREFIX (incl. env-supplied values).
# Deploy ONLY via Jenkinsfile.es4-deploy (digest-pins the :prod tag before apply).
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: '8101'
    prometheus.io/scrape: 'true'
  labels:
    app.kubernetes.io/name: databento-maxpain-service
    app.kubernetes.io/part-of: options-edge
  name: databento-maxpain-service
  namespace: options-edge
spec:
  ports:
  - name: health
    port: 8101
    targetPort: health
  selector:
    app.kubernetes.io/name: databento-maxpain-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: databento-maxpain-service
    app.kubernetes.io/name: databento-maxpain-service
    app.kubernetes.io/part-of: options-edge
  name: databento-maxpain-service
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-maxpain-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: databento-maxpain-service
        app.kubernetes.io/name: databento-maxpain-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
      - env:
        - name: APP_MARKET_DATA_SOURCE
          value: DATABENTO
        - name: HEALTH_PORT
          value: '8101'
        - name: KAFKA_DATABENTO_RAW_TOPIC
          value: options.databento.raw
        - name: KAFKA_DATABENTO_MAXPAIN_TOPIC
          value: options.databento.maxpain
        - name: KAFKA_MAXPAIN_STREAMS_APP_ID
          value: options-databento-maxpain-streams-v2
        - name: KAFKA_MAXPAIN_STREAMS_NUM_THREADS
          value: '2'
        - name: DATABENTO_MAXPAIN_SETTLEMENT_ZONE
          value: America/New_York
        - name: DATABENTO_MAXPAIN_PM_CLOSE
          value: '16:00'
        - name: KAFKA_STREAMS_STATE_DIR
          value: /var/lib/options-edge/kafka-streams
        - name: JAVA_TOOL_OPTIONS
          value: -Xms512m -Xmx2g
        envFrom:
        - configMapRef:
            name: es4-common-env
        - secretRef:
            name: es4-runtime-secrets
        image: 192.168.100.252:5000/options-edge-databento-maxpain:prod
        livenessProbe:
          httpGet:
            path: /health/live
            port: health
          initialDelaySeconds: 20
          periodSeconds: 15
        name: databento-maxpain
        ports:
        - containerPort: 8101
          name: health
        readinessProbe:
          httpGet:
            path: /health/ready
            port: health
          initialDelaySeconds: 20
          periodSeconds: 10
        resources:
          limits:
            memory: 2Gi
          requests:
            cpu: 100m
            memory: 512Mi
        volumeMounts:
        - mountPath: /var/lib/options-edge/kafka-streams
          name: kafka-streams-state
      volumes:
      - emptyDir: {}
        name: kafka-streams-state

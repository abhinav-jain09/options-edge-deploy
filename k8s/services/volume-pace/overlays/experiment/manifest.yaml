# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy path for experiment is the Jenkins experiment job
# (Jenkinsfile.experiment-deploy), which applies the monolithic overlay; there is
# NO digest pinning on this env. Applying this file by hand still violates the
# Jenkins-only deployment rule and can roll out a wrong or stale image.
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: volume-pace-databento-service
    app.kubernetes.io/part-of: options-edge
  name: volume-pace-databento-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: volume-pace-databento-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: volume-pace-databento-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: KAFKA_RAW_TOPIC
              value: options.databento.normalized
            - name: KAFKA_PACE_TOPIC
              value: options.databento.pace
            - name: KAFKA_PACE_STREAMS_APP_ID
              value: options-flow-pace-streams-databento
            - name: KAFKA_PACE_STREAMS_COMMIT_INTERVAL_MS
              value: "1000"
            - name: KAFKA_PACE_STREAMS_NUM_THREADS
              value: "2"
            - name: KAFKA_PACE_STREAMS_CACHE_MAX_BYTES_BUFFERING
              value: "1048576"
            - name: JAVA_TOOL_OPTIONS
              value: -Xms2g -Xmx6g
            - name: PACE_TRAJ_HORIZON_MS
              value: "24300000"
            - name: PACE_TRAJ_GRID_MS
              value: "300000"
            - name: PACE_TRAJ_MAX_FLOOR_AGE_MS
              value: "300000"
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-volume-pace:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: volume-pace
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 9Gi
            requests:
              cpu: 250m
              memory: 2Gi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - name: kafka-streams-state
          persistentVolumeClaim:
            claimName: volume-pace-databento-service-streams-state

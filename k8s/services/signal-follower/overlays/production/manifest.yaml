# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# remaps this render via image-tags/production.yaml then pins; the monolithic
# Jenkinsfile pins from the build-resolved images env (resolve-images.sh).
# Applying this file directly bypasses that pinning and can roll out a wrong or
# stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: signal-follower-spx
    app.kubernetes.io/name: signal-follower-spx
    app.kubernetes.io/part-of: options-edge
  name: signal-follower-spx
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: signal-follower-spx
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: signal-follower-spx
    app.kubernetes.io/name: signal-follower-spx
    app.kubernetes.io/part-of: options-edge
  name: signal-follower-spx
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: signal-follower-spx
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: signal-follower-spx
        app.kubernetes.io/name: signal-follower-spx
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: HEALTH_PORT
              value: "8080"
            - name: SIGNAL_FOLLOWER_SYMBOL
              value: SPX
            - name: LIVENESS_SESSION
              value: rth
            - name: SIGNAL_FOLLOWER_PG_URL
              valueFrom:
                configMapKeyRef:
                  key: POSTGRES_JDBC_URL
                  name: options-edge-config
            - name: SIGNAL_FOLLOWER_PG_USER
              valueFrom:
                configMapKeyRef:
                  key: POSTGRES_USER
                  name: options-edge-config
            - name: SIGNAL_FOLLOWER_PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_PASSWORD
                  name: options-edge-runtime-secrets
            - name: JAVA_TOOL_OPTIONS
              value: -Xms128m -Xmx512m
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-signal-follower:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: signal-follower-spx
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 768Mi
            requests:
              cpu: 50m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: signal-follower-es
    app.kubernetes.io/name: signal-follower-es
    app.kubernetes.io/part-of: options-edge
  name: signal-follower-es
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: signal-follower-es
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: signal-follower-es
    app.kubernetes.io/name: signal-follower-es
    app.kubernetes.io/part-of: options-edge
  name: signal-follower-es
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: signal-follower-es
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: signal-follower-es
        app.kubernetes.io/name: signal-follower-es
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: HEALTH_PORT
              value: "8080"
            - name: SIGNAL_FOLLOWER_SYMBOL
              value: ES
            - name: LIVENESS_SESSION
              value: globex
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: 192.168.100.4:9092
            - name: SIGNAL_FOLLOWER_BY_STRIKE_TOPIC
              value: es.strike-intelligence-by-strike
            - name: SIGNAL_FOLLOWER_DASHBOARD_TOPIC
              value: es.strike-intelligence-dashboard
            - name: SIGNAL_FOLLOWER_PG_URL
              valueFrom:
                configMapKeyRef:
                  key: POSTGRES_JDBC_URL
                  name: options-edge-config
            - name: SIGNAL_FOLLOWER_PG_USER
              valueFrom:
                configMapKeyRef:
                  key: POSTGRES_USER
                  name: options-edge-config
            - name: SIGNAL_FOLLOWER_PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: POSTGRES_PASSWORD
                  name: options-edge-runtime-secrets
            - name: JAVA_TOOL_OPTIONS
              value: -Xms128m -Xmx512m
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-signal-follower:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: signal-follower-es
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 768Mi
            requests:
              cpu: 50m
              memory: 256Mi

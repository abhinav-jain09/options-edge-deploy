# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: volume-sandwich-service
    app.kubernetes.io/part-of: options-edge
  name: volume-sandwich-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: volume-sandwich-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: volume-sandwich-service
    app.kubernetes.io/part-of: options-edge
  name: volume-sandwich-service
  namespace: options-edge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: volume-sandwich-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: volume-sandwich-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: IBKR
            - name: KAFKA_DISPLAY_VOLUME_TOPIC
              value: options.ibkr.display.volume.current
            - name: KAFKA_VOLUME_SANDWICH_CURRENT_TOPIC
              value: options.ibkr.volume-sandwich.current
            - name: KAFKA_VOLUME_SANDWICH_ALERTS_TOPIC
              value: options.ibkr.volume-sandwich.alerts
            - name: KAFKA_VOLUME_SANDWICH_STREAMS_APP_ID
              value: options-flow-volume-sandwich-streams-v2
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-volume-sandwich:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: volume-sandwich
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 3Gi
            requests:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - name: kafka-streams-state
          persistentVolumeClaim:
            claimName: volume-sandwich-service-streams-state
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: volume-sandwich-databento-service
    app.kubernetes.io/part-of: options-edge
  name: volume-sandwich-databento-service
  namespace: options-edge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: volume-sandwich-databento-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: volume-sandwich-databento-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: KAFKA_DISPLAY_VOLUME_TOPIC
              value: options.databento.display.volume.current
            - name: KAFKA_VOLUME_SANDWICH_CURRENT_TOPIC
              value: options.databento.volume-sandwich.current
            - name: KAFKA_VOLUME_SANDWICH_ALERTS_TOPIC
              value: options.databento.volume-sandwich.alerts
            - name: KAFKA_VOLUME_SANDWICH_STREAMS_APP_ID
              value: options-flow-volume-sandwich-streams-v2-databento
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-volume-sandwich:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: volume-sandwich
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 3Gi
            requests:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - name: kafka-streams-state
          persistentVolumeClaim:
            claimName: volume-sandwich-databento-service-streams-state

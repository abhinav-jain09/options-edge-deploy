# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a PRE-PIN
# render: the image ref below is the BASE (dev-registry) ref, NOT this env's image.
# The ONLY deploy paths are the Jenkins jobs (Jenkinsfile.service-deploy -> 
# scripts/deploy/service-deploy.sh, or the monolithic Jenkinsfile), which remap the
# image to image-tags/experiment.yaml and digest-pin it fail-closed BEFORE any apply.
# Applying this file directly to production would roll out the dev image.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8099"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: databento-mission-sandwich-service
    app.kubernetes.io/part-of: options-edge
  name: databento-mission-sandwich-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8099
      targetPort: health
  selector:
    app.kubernetes.io/name: databento-mission-sandwich-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: databento-mission-sandwich-service
    app.kubernetes.io/part-of: options-edge
    oe.replay/determinism: event-time
    oe.replay/include: "true"
    oe.replay/side-effects: "false"
    oe.replay/state: ephemeral
    oe.replay/topic-remap: required
  name: databento-mission-sandwich-service
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-mission-sandwich-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databento-mission-sandwich-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: HEALTH_PORT
              value: "8099"
            - name: KAFKA_MISSION_SANDWICH_VOLUME_TOPIC
              value: display.volume.current
            - name: KAFKA_MISSION_SANDWICH_PACE_TOPIC
              value: options.databento.pace.mission
            - name: KAFKA_MISSION_SANDWICH_PRESSURE_TOPIC
              value: options.databento.market-pressure.mission
            - name: KAFKA_MISSION_SANDWICH_OUTPUT_TOPIC
              value: options.databento.sandwich.mission
            - name: KAFKA_MISSION_SANDWICH_STREAMS_APP_ID
              value: options-edge-databento-mission-sandwich-v1
            - name: KAFKA_MISSION_SANDWICH_STREAMS_NUM_THREADS
              value: "4"
            - name: KAFKA_MISSION_SANDWICH_OUTPUT_INTERVAL_MS
              value: "1000"
            - name: KAFKA_MISSION_SANDWICH_STALE_AFTER_MS
              value: "15000"
            - name: MISSION_SANDWICH_REQUIRE_DATABENTO_SOURCE
              value: "true"
            - name: MISSION_SANDWICH_ENABLE_PACE
              value: "true"
            - name: MISSION_SANDWICH_ENABLE_PRESSURE
              value: "true"
            - name: MISSION_SANDWICH_ENABLE_GEX
              value: "false"
            - name: MISSION_SANDWICH_ENABLE_PARTICIPANT_FLOW
              value: "false"
            - name: MISSION_SANDWICH_STRIKE_WIDTH_POINTS
              value: "5.0"
            - name: MISSION_SANDWICH_DYNAMIC_STRIKE_WIDTH
              value: "true"
            - name: MISSION_SANDWICH_MIN_SIDE_VOLUME
              value: "100"
            - name: MISSION_SANDWICH_MIN_MID_VOLUME
              value: "10"
            - name: MISSION_SANDWICH_MIN_VOLUME_STRENGTH
              value: "1.25"
            - name: MISSION_SANDWICH_MAX_RANKED_ZONES
              value: "50"
            - name: MISSION_SANDWICH_ATM_DISTANCE_POINTS
              value: "5.0"
            - name: MISSION_SANDWICH_NEAR_ATM_DISTANCE_POINTS
              value: "15.0"
            - name: MISSION_SANDWICH_BREAKOUT_DISTANCE_POINTS
              value: "2.0"
            - name: MISSION_SANDWICH_COMPRESSION_MIN_SECONDS
              value: "30"
            - name: JAVA_TOOL_OPTIONS
              value: -Xms1g -Xmx4g
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-databento-mission-sandwich:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: databento-mission-sandwich
          ports:
            - containerPort: 8099
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 5632Mi
            requests:
              cpu: 250m
              memory: 1Gi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - name: kafka-streams-state
          persistentVolumeClaim:
            claimName: databento-mission-sandwich-service-streams-state

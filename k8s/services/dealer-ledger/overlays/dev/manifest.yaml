# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/dev render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# keeps this env-correct render ref then pins (image-tags/dev.yaml is
# informational here); the monolithic Jenkinsfile pins from the build-resolved
# images env (resolve-images.sh). Applying this file directly bypasses that
# pinning and can roll out a wrong or stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: dealer-ledger-service
    app.kubernetes.io/name: dealer-ledger-service
    app.kubernetes.io/part-of: options-edge
  name: dealer-ledger-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8113
      targetPort: health
  selector:
    app.kubernetes.io/name: dealer-ledger-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: dealer-ledger-service
    app.kubernetes.io/name: dealer-ledger-service
    app.kubernetes.io/part-of: options-edge
  name: dealer-ledger-service
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dealer-ledger-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: dealer-ledger-service
        app.kubernetes.io/name: dealer-ledger-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: DEALER_LEDGER_ABSORB_MIN_FRACTION
              value: "0.50"
            - name: DEALER_LEDGER_ABSORB_MIN_NOTIONAL
              value: "2000000"
            - name: DEALER_LEDGER_ABSORB_MIN_WAVES
              value: "2"
            - name: DEALER_LEDGER_WAVE_MIN_NOTIONAL
              value: "750000"
            - name: DEALER_LEDGER_WAVE_GAP_MS
              value: "90000"
            - name: DEALER_LEDGER_UNWIND_ARM_MIN
              value: "75000000"
            - name: DEALER_LEDGER_FAST_GAMMA_MIN
              value: "2500000"
            - name: DEALER_LEDGER_CUSHION_GAMMA_MIN
              value: "1500000"
            - name: DEALER_LEDGER_REGIME_GAMMA_EPS
              value: "1000000"
            - name: DEALER_LEDGER_FAST_LIQUIDITY_MAX
              value: "50"
            - name: DEALER_LEDGER_DEFENSE_MIN_SELL_CONTRACTS
              value: "350"
            - name: DEALER_LEDGER_DEFENSE_TOUCH_BAND_PTS
              value: "5.0"
            - name: DEALER_LEDGER_PIN_RANGE_PTS
              value: "40.0"
            - name: DEALER_LEDGER_PIN_WINDOW_MS
              value: "3600000"
            - name: DEALER_LEDGER_CLUSTER_RANGE_PTS
              value: "15.0"
            - name: DEALER_LEDGER_CLUSTER_MEMBER_MIN_FRAC
              value: "0.20"
            - name: DEALER_LEDGER_STALL_FLOW_USD
              value: "true"
            - name: DEALER_LEDGER_STALL_MINUTES
              value: "8"
            - name: DEALER_LEDGER_UNWIND_SPENT_PCT
              value: "0.80"
            - name: DEALER_LEDGER_REARM_FLOW_USD
              value: -Xms1024m -Xmx6144m
            - name: DEALER_LEDGER_REARM_MINUTES
              value: "12"
            - name: DEALER_LEDGER_REARM_COOLOFF_MS
              value: "600000"
            - name: DEALER_LEDGER_MIN_LEDGER_NOTIONAL
              value: "7500000"
            - name: DEALER_LEDGER_TRIGGER_MIN_CONFIDENCE
              value: MEDIUM
            - name: DEALER_LEDGER_GROSS_NET_MAX
              value: "6.0"
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: HEALTH_PORT
              value: "8113"
            - name: DEALER_LEDGER_INPUT_STRIKE_FLOW_TOPIC
              value: options.databento.strike-flow
            - name: DEALER_LEDGER_INPUT_GREEKS_TOPIC
              value: options.databento.gex.strike
            - name: DEALER_LEDGER_INPUT_SNAPSHOT_TOPIC
              value: options.databento.raw
            - name: DEALER_LEDGER_INPUT_HEATMAP_TOPIC
              value: strike-liquidity-heatmap-dashboard
            - name: DEALER_LEDGER_INPUT_DELTA_FLOW_TOPIC
              value: delta-flow-session
            - name: DEALER_LEDGER_OUTPUT_PROFILE_TOPIC
              value: dealer-ledger-profile
            - name: DEALER_LEDGER_OUTPUT_STATE_TOPIC
              value: dealer-ledger-state
            - name: DEALER_LEDGER_STREAMS_APP_ID
              value: options-edge-dealer-ledger-service-v1
            - name: DEALER_LEDGER_STREAMS_COMMIT_INTERVAL_MS
              value: "1000"
            - name: DEALER_LEDGER_STREAMS_AUTO_OFFSET_RESET
              value: latest
            - name: DEALER_LEDGER_STREAMS_NUM_THREADS
              value: "1"
            - name: DEALER_LEDGER_EOS_ENABLED
              value: "true"
            - name: KAFKA_STREAMS_STATE_DIR
              value: /var/lib/options-edge/kafka-streams
            - name: DEALER_LEDGER_ENABLED
              value: "true"
            - name: DEALER_LEDGER_ADVISORY_ENABLED
              value: "false"
            - name: DEALER_LEDGER_UI_VISIBLE
              value: "true"
            - name: DEALER_LEDGER_CALIBRATION_QUALITY
              value: SHADOW
            - name: DEALER_LEDGER_QUOTE_STALE_MS
              value: "600000"
            - name: JAVA_TOOL_OPTIONS
              value: -Xms384m -Xmx1024m
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-dealer-ledger:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: dealer-ledger
          ports:
            - containerPort: 8113
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 8Gi
            requests:
              cpu: 100m
              memory: 2Gi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state

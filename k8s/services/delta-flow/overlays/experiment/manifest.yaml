# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs (Jenkinsfile.service-deploy ->
# scripts/deploy/service-deploy.sh, or the monolithic Jenkinsfile), which remap the
# image via image-tags/experiment.yaml and digest-pin it fail-closed BEFORE any apply.
# Applying this file directly bypasses that pinning and can roll out a wrong or
# stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: delta-flow-service
    app.kubernetes.io/name: delta-flow-service
    app.kubernetes.io/part-of: options-edge
  name: delta-flow-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8110
      targetPort: health
  selector:
    app.kubernetes.io/name: delta-flow-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: delta-flow-service
    app.kubernetes.io/name: delta-flow-service
    app.kubernetes.io/part-of: options-edge
  name: delta-flow-service
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: delta-flow-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: delta-flow-service
        app.kubernetes.io/name: delta-flow-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: HEALTH_PORT
              value: "8110"
            - name: DELTA_FLOW_INPUT_TCBBO_TOPIC
              value: options.opra.tcbbo
            - name: DELTA_FLOW_INPUT_GEX_TOPIC
              value: options.databento.gex.strike
            - name: DELTA_FLOW_STREAMS_APP_ID
              value: options-edge-delta-flow-service-v1
            - name: DELTA_FLOW_STREAMS_COMMIT_INTERVAL_MS
              value: "1000"
            - name: DELTA_FLOW_STREAMS_AUTO_OFFSET_RESET
              value: latest
            - name: DELTA_FLOW_STREAMS_NUM_THREADS
              value: "1"
            - name: KAFKA_STREAMS_STATE_DIR
              value: /var/lib/options-edge/kafka-streams
            - name: JAVA_TOOL_OPTIONS
              value: -Xms256m -Xmx768m
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-delta-flow:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: delta-flow
          ports:
            - containerPort: 8110
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 384Mi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state

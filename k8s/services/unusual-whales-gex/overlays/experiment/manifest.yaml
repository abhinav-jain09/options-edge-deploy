# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a PRE-PIN
# render: the image ref below is the BASE (dev-registry) ref, NOT this env's image.
# The ONLY deploy paths are the Jenkins jobs (Jenkinsfile.service-deploy ->
# scripts/deploy/service-deploy.sh, or the monolithic Jenkinsfile), which remap the
# image to image-tags/experiment.yaml and digest-pin it fail-closed BEFORE any apply.
# Applying this file directly to production would roll out the dev image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: unusual-whales-gex-service
    app.kubernetes.io/part-of: options-edge
  name: unusual-whales-gex-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: unusual-whales-gex-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: unusual-whales-gex-service
    app.kubernetes.io/part-of: options-edge
  name: unusual-whales-gex-service
  namespace: options-edge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: unusual-whales-gex-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: unusual-whales-gex-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: UNUSUAL_WHALES_API_KEY
              valueFrom:
                secretKeyRef:
                  key: unusual-whales-api-key
                  name: options-edge-secrets
                  optional: true
            - name: KAFKA_MARKET_DATA_SELECTION_TOPIC
              value: options.marketdata.selection
            - name: KAFKA_UNUSUAL_WHALES_GEX_TOPIC
              value: options.ibkr.unusualwhales.gex.strike
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-unusual-whales-gex:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: unusual-whales-gex
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 768Mi
            requests:
              cpu: 100m
              memory: 256Mi

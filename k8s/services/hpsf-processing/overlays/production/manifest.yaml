# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a PRE-PIN
# render: the image ref below is the BASE (dev-registry) ref, NOT this env's image.
# The ONLY deploy paths are the Jenkins jobs (Jenkinsfile.service-deploy ->
# scripts/deploy/service-deploy.sh, or the monolithic Jenkinsfile), which remap the
# image to image-tags/production.yaml and digest-pin it fail-closed BEFORE any apply.
# Applying this file directly to production would roll out the dev image.
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: hpsf-stage-a
    app.kubernetes.io/name: hpsf-stage-a-service
    app.kubernetes.io/part-of: options-edge
    oe.replay/determinism: event-time
    oe.replay/include: "true"
    oe.replay/side-effects: "false"
    oe.replay/state: ephemeral
    oe.replay/topic-remap: required
  name: hpsf-stage-a-service
  namespace: options-edge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: hpsf-stage-a-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: hpsf-stage-a
        app.kubernetes.io/name: hpsf-stage-a-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: HPSF_STATE_STORE_TYPE
              value: persistent
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: HPSF_STAGE_ROLE
              value: STAGE_A
            - name: HPSF_STAGE_A_ENABLED
              value: "true"
            - name: HPSF_STAGE_B_ENABLED
              value: "false"
            - name: HPSF_ENABLED
              value: "true"
            - name: HPSF_TOPOLOGY_ENABLED
              value: "true"
            - name: HPSF_MODE
              value: LIVE_SIGNAL_ONLY
            - name: HPSF_STREAMS_APPLICATION_ID
              value: options-edge-hpsf-stage-a-v2-1
            - name: HPSF_STREAMS_REPLICATION_FACTOR
              value: "1"
            - name: HPSF_STREAMS_NUM_STANDBY_REPLICAS
              value: "0"
            - name: HPSF_STREAMS_STATE_DIR
              value: /home/options-edge/data/kafka-streams/hpsf/stage-a
            - name: HPSF_STATE_CHANGELOG_ENABLED
              value: "true"
            - name: HPSF_ORDER_PLACEMENT_ENABLED
              value: "false"
            - name: ORDER_PLACEMENT_ENABLED
              value: "false"
            - name: JAVA_TOOL_OPTIONS
              value: -Xmx4g
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-hpsf-processing:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pgrep -f 'app.jar|options-edge-hpsf-processing' >/dev/null
            initialDelaySeconds: 30
            periodSeconds: 15
          name: hpsf-stage-a
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pgrep -f 'app.jar|options-edge-hpsf-processing' >/dev/null
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 7Gi
            requests:
              cpu: 500m
              memory: 4Gi
          volumeMounts:
            - mountPath: /home/options-edge/data/kafka-streams/hpsf
              name: hpsf-kafka-streams-state
      volumes:
        - name: hpsf-kafka-streams-state
          persistentVolumeClaim:
            claimName: hpsf-stage-a-service-streams-state
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: hpsf-stage-b
    app.kubernetes.io/name: hpsf-stage-b-service
    app.kubernetes.io/part-of: options-edge
    oe.replay/determinism: event-time
    oe.replay/include: "true"
    oe.replay/side-effects: "false"
    oe.replay/state: ephemeral
    oe.replay/topic-remap: required
  name: hpsf-stage-b-service
  namespace: options-edge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: hpsf-stage-b-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: hpsf-stage-b
        app.kubernetes.io/name: hpsf-stage-b-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: HPSF_STATE_STORE_TYPE
              value: persistent
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: HPSF_STAGE_ROLE
              value: STAGE_B
            - name: HPSF_STAGE_A_ENABLED
              value: "false"
            - name: HPSF_STAGE_B_ENABLED
              value: "true"
            - name: HPSF_ENABLED
              value: "true"
            - name: HPSF_TOPOLOGY_ENABLED
              value: "true"
            - name: HPSF_MODE
              value: LIVE_SIGNAL_ONLY
            - name: HPSF_STREAMS_APPLICATION_ID
              value: options-edge-hpsf-stage-b-v2-1
            - name: HPSF_STREAMS_REPLICATION_FACTOR
              value: "1"
            - name: HPSF_STREAMS_NUM_STANDBY_REPLICAS
              value: "0"
            - name: HPSF_STREAMS_STATE_DIR
              value: /home/options-edge/data/kafka-streams/hpsf/stage-b
            - name: HPSF_STATE_CHANGELOG_ENABLED
              value: "true"
            - name: HPSF_ORDER_PLACEMENT_ENABLED
              value: "false"
            - name: ORDER_PLACEMENT_ENABLED
              value: "false"
            - name: JAVA_TOOL_OPTIONS
              value: -Xmx3g
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-hpsf-processing:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pgrep -f 'app.jar|options-edge-hpsf-processing' >/dev/null
            initialDelaySeconds: 30
            periodSeconds: 15
          name: hpsf-stage-b
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - pgrep -f 'app.jar|options-edge-hpsf-processing' >/dev/null
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 5Gi
            requests:
              cpu: 500m
              memory: 3Gi
          volumeMounts:
            - mountPath: /home/options-edge/data/kafka-streams/hpsf
              name: hpsf-kafka-streams-state
      volumes:
        - name: hpsf-kafka-streams-state
          persistentVolumeClaim:
            claimName: hpsf-stage-b-service-streams-state

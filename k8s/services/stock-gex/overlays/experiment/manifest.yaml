# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy path for experiment is the Jenkins experiment job
# (Jenkinsfile.experiment-deploy), which applies the monolithic overlay; there is
# NO digest pinning on this env. Applying this file by hand still violates the
# Jenkins-only deployment rule and can roll out a wrong or stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: stock-gex
    app.kubernetes.io/name: stock-gex-service
    app.kubernetes.io/part-of: options-edge
  name: stock-gex-service
  namespace: options-edge
spec:
  ports:
    - name: api
      port: 8021
      targetPort: api
    - name: health
      port: 8022
      targetPort: health
  selector:
    app.kubernetes.io/name: stock-gex-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: stock-gex
    app.kubernetes.io/name: stock-gex-service
    app.kubernetes.io/part-of: options-edge
  name: stock-gex-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: stock-gex-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: stock-gex
        app.kubernetes.io/name: stock-gex-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - command:
            - options-edge-stock-gex-service
          env:
            - name: DATABENTO_API_KEY
              valueFrom:
                secretKeyRef:
                  key: DATABENTO_API_KEY
                  name: options-edge-databento-feed-env
            - name: STOCK_GEX_OI_INDEX
              value: /oi-index/stock-gex-oi-index.json.gz
            # THE READ SWITCH. Empty (or absent) means the frozen boards are read from the
            # directory below, exactly as today. Set, and they are read from Postgres instead.
            # Both stores answer with the same methods and the same error names, so nothing
            # downstream can tell which one served a request -- which is what makes the switch a
            # variable rather than a release, and what lets it be taken back out.
            #
            # ⚠️ DO NOT SET THIS WHILE THE MARKET IS OPEN. The live board path is untouched by it,
            # but the after-hours view is what the Gamma Lab falls back to, and a switch is worth
            # making when a mistake costs an empty panel rather than a trading session.
            - name: STOCK_GEX_CLOSE_DB_DSN
              valueFrom:
                secretKeyRef:
                  name: options-edge-databento-feed-env
                  key: STOCK_GEX_CLOSE_DB_DSN
                  optional: true
            # KEPT, deliberately, after the switch as well. The files remain the fallback until
            # the database path has run for long enough to be trusted, and removing this line is
            # the LAST step of the migration, not part of the switch.
            - name: STOCK_GEX_CLOSE_DIR
              value: /oi-index/close
            - name: HEALTH_PORT
              value: "8022"
          image: 192.168.100.252:5000/options-edge-stock-gex:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: stock-gex
          ports:
            - containerPort: 8021
              name: api
            - containerPort: 8022
              name: health
          readinessProbe:
            failureThreshold: 30
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 200m
              memory: 512Mi
          volumeMounts:
            - mountPath: /oi-index
              name: oi-index
              readOnly: true
      volumes:
        - hostPath:
            path: /home/options-edge/stock-gex-oi
            type: DirectoryOrCreate
          name: oi-index

# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/dev render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# keeps this env-correct render ref then pins (image-tags/dev.yaml is
# informational here); the monolithic Jenkinsfile pins from the build-resolved
# images env (resolve-images.sh). Applying this file directly bypasses that
# pinning and can roll out a wrong or stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: stock-gex
    app.kubernetes.io/name: stock-gex-service
    app.kubernetes.io/part-of: options-edge
  name: stock-gex-service
  namespace: options-edge
spec:
  ports:
    - name: api
      port: 8021
      targetPort: api
    - name: health
      port: 8022
      targetPort: health
  selector:
    app.kubernetes.io/name: stock-gex-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: stock-gex
    app.kubernetes.io/name: stock-gex-service
    app.kubernetes.io/part-of: options-edge
  name: stock-gex-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: stock-gex-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: stock-gex
        app.kubernetes.io/name: stock-gex-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - command:
            - options-edge-stock-gex-service
          env:
            - name: DATABENTO_API_KEY
              valueFrom:
                secretKeyRef:
                  key: DATABENTO_API_KEY
                  name: options-edge-databento-feed-env
            - name: STOCK_GEX_OI_INDEX
              value: /oi-index/stock-gex-oi-index.json.gz
            - name: HEALTH_PORT
              value: "8022"
          image: host.docker.internal:5001/options-edge-stock-gex:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: stock-gex
          ports:
            - containerPort: 8021
              name: api
            - containerPort: 8022
              name: health
          readinessProbe:
            failureThreshold: 30
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 200m
              memory: 512Mi
          volumeMounts:
            - mountPath: /oi-index
              name: oi-index
              readOnly: true
      volumes:
        - hostPath:
            path: /var/lib/options-edge/stock-gex-oi
            type: DirectoryOrCreate
          name: oi-index

# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# remaps this render via image-tags/production.yaml then pins; the monolithic
# Jenkinsfile pins from the build-resolved images env (resolve-images.sh).
# Applying this file directly bypasses that pinning and can roll out a wrong or
# stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: stock-gex
    app.kubernetes.io/name: stock-gex-service
    app.kubernetes.io/part-of: options-edge
  name: stock-gex-service
  namespace: options-edge
spec:
  ports:
    - name: api
      port: 8021
      targetPort: api
    - name: health
      port: 8022
      targetPort: health
  selector:
    app.kubernetes.io/name: stock-gex-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: stock-gex
    app.kubernetes.io/name: stock-gex-service
    app.kubernetes.io/part-of: options-edge
  name: stock-gex-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: stock-gex-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: stock-gex
        app.kubernetes.io/name: stock-gex-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - command:
            - options-edge-stock-gex-service
          env:
            - name: DATABENTO_API_KEY
              valueFrom:
                secretKeyRef:
                  key: DATABENTO_API_KEY
                  name: options-edge-databento-feed-env
            - name: STOCK_GEX_OI_INDEX
              value: /oi-index/stock-gex-oi-index.json.gz
            # KEPT, deliberately, after the switch as well. The files remain the fallback until
            # the database path has run for long enough to be trusted, and removing this line is
            # the LAST step of the migration, not part of the switch.
            - name: STOCK_GEX_CLOSE_DIR
              value: /oi-index/close
            - name: HEALTH_PORT
              value: "8022"
            # ⚠️ THE CONNECTION STRING IS INLINE, PASSWORD AND ALL, BY EXPLICIT DECISION
            # (USER 2026-08-27, after the alternatives were laid out). It is here rather than in a
            # Secret because options-edge is guarded by the ValidatingAdmissionPolicy
            # 'options-edge-jenkins-only-workloads' -- nothing may write a Secret there except the
            # Jenkins deployer, and the only job that does is the monolithic options-edge-deploy,
            # whose last eight runs all failed or were aborted.
            #
            # WHAT THIS COSTS, recorded so nobody has to rediscover it: a credential in git is
            # PERMANENT. Deleting the line later does not remove it from history, and this repo is
            # cloned to every worktree on the build host and to Jenkins. The way to retire this
            # value is to ROTATE the bo-app-postgres password, not to edit this file.
            #
            # PRODUCTION ONLY, deliberately: it is not in k8s/base, because the base is also
            # rendered for dev and experiment and they would then WRITE INTO THE PRODUCTION
            # DATABASE -- a far worse outcome than the one this patch exists to solve.
            - name: STOCK_GEX_CLOSE_DB_DSN
              value: "postgresql://bleedingoptions:fAE80oxjlQ75edJe5nmfTLoIDJerlkddoFRm@bo-app-postgres-db.bleedingoptions.svc.cluster.local:5432/bleedingoptions"
          image: 192.168.100.252:5000/options-edge-stock-gex:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: stock-gex
          ports:
            - containerPort: 8021
              name: api
            - containerPort: 8022
              name: health
          readinessProbe:
            failureThreshold: 30
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 200m
              memory: 512Mi
          volumeMounts:
            - mountPath: /oi-index
              name: oi-index
              readOnly: true
      volumes:
        - hostPath:
            path: /home/options-edge/stock-gex-oi
            type: DirectoryOrCreate
          name: oi-index

# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/dev render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# keeps this env-correct render ref then pins (image-tags/dev.yaml is
# informational here); the monolithic Jenkinsfile pins from the build-resolved
# images env (resolve-images.sh). Applying this file directly bypasses that
# pinning and can roll out a wrong or stale image.
apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    options-edge/blocked-on: 'Production. This is dev-only: publishing the operator''s live positions to a production topic is a deliberate decision, and the consumer''s own production gates (MIN_ACTIONABLE_LEAD_MS, operator-approved thresholds) are still open.'
    options-edge/purpose: Publishes the operator's LIVE IBKR short option legs to positions.approach.registry, which approach-monitor consumes. That topic had no producer at all, so the monitor — which passed its Gate 2 implementation review — would have consumed an empty topic and honestly reported POSITION_SOURCE_UNAVAILABLE for ever.
    options-edge/single-writer: EXACTLY ONE REPLICA, with a stable transactional id. Two publishers would both be authorities on the same account and would take turns assigning episode identities; the consumer treats an episode key as an identity carrying a terminal frontier, a severity, a touch and an alert latch. A registry with two authorities is worse than one that is briefly down.
  labels:
    app.kubernetes.io/name: approach-position-publisher
    app.kubernetes.io/part-of: options-edge
  name: approach-position-publisher
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: approach-position-publisher
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: approach-position-publisher
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: HEALTH_PORT
              value: "8140"
            - name: IB_HOST
              value: host.docker.internal
            - name: IB_PORT
              value: "4002"
            - name: IBKR_APPROACH_CLIENT_ID
              value: "951"
            - name: TRANSACTIONAL_ID
              value: approach-position-publisher-1
            - name: JAVA_TOOL_OPTIONS
              value: -XX:MaxRAMPercentage=70
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-approach-position-publisher:dev
          imagePullPolicy: Always
          livenessProbe:
            failureThreshold: 4
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 30
            periodSeconds: 15
          name: approach-position-publisher
          ports:
            - containerPort: 8140
              name: health
          readinessProbe:
            failureThreshold: 3
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 512Mi
            requests:
              cpu: 50m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - mountPath: /tmp
              name: tmp
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - emptyDir: {}
          name: tmp

# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/dev render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# keeps this env-correct render ref then pins (image-tags/dev.yaml is
# informational here); the monolithic Jenkinsfile pins from the build-resolved
# images env (resolve-images.sh). Applying this file directly bypasses that
# pinning and can roll out a wrong or stale image.
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: short-premium-agent-service
    app.kubernetes.io/name: short-premium-agent-service
    app.kubernetes.io/part-of: options-edge
  name: short-premium-agent-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: short-premium-agent-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: short-premium-agent-service
        app.kubernetes.io/name: short-premium-agent-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: SP_ENABLED
              value: "1"
            - name: SP_BACKEND
              value: ollama
            - name: SP_OLLAMA_URL
              value: http://host.docker.internal:11434
            - name: SP_OLLAMA_MODEL
              value: qwen2.5:72b
            - name: SP_EMIT_KAFKA
              value: "1"
            - name: SP_HEALTH_PORT
              value: "8130"
            - name: SP_MODEL
              value: claude-opus-4-8
            - name: SP_MAX_TOKENS
              value: "4000"
            - name: SP_WING
              value: "5"
            - name: SP_MIN_CREDIT
              value: "2.50"
            - name: SP_MAX_TRADES
              value: "2"
            - name: SP_QTY
              value: "1"
            - name: SP_SYMBOL
              value: SPX
            - name: SP_CADENCE
              value: "60"
            - name: SP_CADENCE_T10_5
              value: "60"
            - name: SP_CADENCE_FINAL5
              value: "60"
            - name: SP_OPEN
              value: "09:35"
            - name: SP_CLOSE
              value: "16:00"
            - name: SP_MARKET_GATE
              value: "1"
            - name: SP_REQUIRE_REAL
              value: "0"
            - name: SP_MAX_AGE
              value: "20"
            - name: SP_REC_TOPIC
              value: options.short-premium.recommendation
            - name: SP_BOOK
              value: /data/paper_book.json
            - name: SP_AUDIT_DIR
              value: /data/audit
            - name: SP_COST
              value: /data/daily_cost.json
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-short-premium-agent:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          name: agent-a
          ports:
            - containerPort: 8130
              name: health
          readinessProbe:
            httpGet:
              path: /health
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              memory: 512Mi
            requests:
              cpu: 50m
              memory: 256Mi
          volumeMounts:
            - mountPath: /data
              name: sp-data
        - command:
            - python
            - run_agent_b.py
            - --daemon
          env:
            - name: SP_ENABLED
              value: "1"
            - name: SP_BACKEND
              value: ollama
            - name: SP_OLLAMA_URL
              value: http://host.docker.internal:11434
            - name: SP_OLLAMA_MODEL
              value: qwen2.5:72b
            - name: SP_MODEL
              value: claude-opus-4-8
            - name: SP_SYMBOL
              value: SPX
            - name: SP_EOD_TIME
              value: "16:05"
            - name: SP_BOOK
              value: /data/paper_book.json
            - name: SP_AUDIT_DIR
              value: /data/audit
            - name: SP_COST
              value: /data/daily_cost.json
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-short-premium-agent:dev
          imagePullPolicy: Always
          name: agent-b
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 25m
              memory: 128Mi
          volumeMounts:
            - mountPath: /data
              name: sp-data
      volumes:
        - emptyDir: {}
          name: sp-data

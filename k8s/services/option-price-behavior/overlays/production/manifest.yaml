# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# remaps this render via image-tags/production.yaml then pins; the monolithic
# Jenkinsfile pins from the build-resolved images env (resolve-images.sh).
# Applying this file directly bypasses that pinning and can roll out a wrong or
# stale image.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: option-price-behavior-service
    app.kubernetes.io/part-of: options-edge
  name: option-price-behavior-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: option-price-behavior-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: option-price-behavior-service
    app.kubernetes.io/part-of: options-edge
  name: option-price-behavior-service
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: option-price-behavior-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: option-price-behavior-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: OPTION_PRICE_BEHAVIOR_INPUT_TCBBO_TOPIC
              value: options.opra.tcbbo
            - name: OPTION_PRICE_BEHAVIOR_INPUT_GREEKS_TOPIC
              value: options.databento.gex.strike
            - name: OPTION_PRICE_BEHAVIOR_INPUT_UNDERLYING_TOPIC
              value: underlying.spx.price
            - name: OPTION_PRICE_BEHAVIOR_DASHBOARD_TOPIC
              value: option-price-behavior-dashboard
            - name: OPTION_PRICE_BEHAVIOR_STREAMS_APP_ID
              value: option-price-behavior-service-v1
            - name: OPTION_PRICE_BEHAVIOR_STREAMS_NUM_THREADS
              value: "1"
            - name: OPTION_PRICE_BEHAVIOR_STREAMS_AUTO_OFFSET_RESET
              value: latest
            - name: JAVA_TOOL_OPTIONS
              value: -Xms512m -Xmx2g
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-option-price-behavior:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: option-price-behavior
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 2Gi
            requests:
              cpu: 250m
              memory: 768Mi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state
---
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: option-price-behavior-service-v2
    app.kubernetes.io/part-of: options-edge
  name: option-price-behavior-service-v2
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: option-price-behavior-service-v2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: option-price-behavior-service-v2
    app.kubernetes.io/part-of: options-edge
  name: option-price-behavior-service-v2
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: option-price-behavior-service-v2
  template:
    metadata:
      labels:
        app.kubernetes.io/name: option-price-behavior-service-v2
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: OPB_SERVICE_VARIANT
              value: v2
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: OPTION_PRICE_BEHAVIOR_INPUT_TCBBO_TOPIC
              value: options.opra.tcbbo
            - name: OPTION_PRICE_BEHAVIOR_INPUT_GREEKS_TOPIC
              value: options.databento.gex.strike
            - name: OPTION_PRICE_BEHAVIOR_INPUT_UNDERLYING_TOPIC
              value: underlying.spx.price
            - name: OPTION_PRICE_BEHAVIOR_V2_BY_OPTION_TOPIC
              value: option-price-behavior-v2-by-option
            - name: OPTION_PRICE_BEHAVIOR_V2_SESSION_TOPIC
              value: option-price-behavior-v2-session
            - name: OPTION_PRICE_BEHAVIOR_V2_STREAMS_APP_ID
              value: option-price-behavior-service-v2
            - name: OPTION_PRICE_BEHAVIOR_V2_STREAMS_NUM_THREADS
              value: "1"
            - name: OPTION_PRICE_BEHAVIOR_V2_STREAMS_AUTO_OFFSET_RESET
              value: earliest
            - name: KAFKA_STREAMS_STATE_DIR
              value: /var/lib/options-edge/kafka-streams
            - name: JAVA_TOOL_OPTIONS
              value: -Xms512m -Xmx2g
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-option-price-behavior:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: option-price-behavior
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 2Gi
            requests:
              cpu: 250m
              memory: 768Mi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state

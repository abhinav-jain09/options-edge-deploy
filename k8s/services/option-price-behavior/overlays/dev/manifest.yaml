# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/dev render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# keeps this env-correct render ref then pins (image-tags/dev.yaml is
# informational here); the monolithic Jenkinsfile pins from the build-resolved
# images env (resolve-images.sh). Applying this file directly bypasses that
# pinning and can roll out a wrong or stale image.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: option-price-behavior-service
    app.kubernetes.io/part-of: options-edge
  name: option-price-behavior-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: option-price-behavior-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: option-price-behavior-service
    app.kubernetes.io/part-of: options-edge
  name: option-price-behavior-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: option-price-behavior-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: option-price-behavior-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: OPB_V2_ROLLING_WINDOWS_ENABLED
              value: "false"
            - name: JAVA_TOOL_OPTIONS
              # Kafka Streams keeps RocksDB block/index memory outside the Java heap. Four stream
              # threads restoring 32 partitions exceeded the old 4Gi container limit even with a
              # 2Gi heap, causing repeated exit-137 restarts and a blank OPB board. Match the stable
              # production envelope so heap plus native RocksDB state has adequate headroom.
              value: -Xms512m -Xmx3g
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: OPTION_PRICE_BEHAVIOR_INPUT_TCBBO_TOPIC
              value: options.opra.tcbbo
            - name: OPTION_PRICE_BEHAVIOR_INPUT_GREEKS_TOPIC
              value: options.databento.gex.strike
            - name: OPTION_PRICE_BEHAVIOR_INPUT_UNDERLYING_TOPIC
              value: underlying.spx.price
            - name: OPTION_PRICE_BEHAVIOR_V2_BY_OPTION_TOPIC
              value: option-price-behavior-v2-by-option
            - name: OPTION_PRICE_BEHAVIOR_V2_SESSION_TOPIC
              value: option-price-behavior-v2-session
            - name: OPTION_PRICE_BEHAVIOR_V2_STREAMS_APP_ID
              value: option-price-behavior-service-v2r4
            - name: OPTION_PRICE_BEHAVIOR_V2_STREAMS_NUM_THREADS
              value: "4"
            - name: OPTION_PRICE_BEHAVIOR_V2_STREAMS_AUTO_OFFSET_RESET
              value: earliest
            - name: KAFKA_STREAMS_STATE_DIR
              value: /var/lib/options-edge/kafka-streams
            - name: OPB_V2_Z_THRESHOLD
              value: "2.05"
            - name: OPB_V2_RESIDUAL_FLOOR_VAR
              value: "0.0004"
            - name: OPB_V2_EWMA_HALFLIFE_MS
              value: "300000"
            - name: OPB_V2_WINSOR_K
              value: "7.0"
            - name: OPB_V2_K_QUOTE_AGE
              value: "0.12"
            - name: OPB_V2_K_GREEK_AGE
              value: "0.025"
            - name: OPB_V2_HALF_SPREAD_WEIGHT
              value: "0.38"
            - name: OPB_V2_TICK_WEIGHT
              value: "0.50"
            - name: OPB_V2_TICK_SIZE
              value: "0.05"
            - name: OPB_V2_WEIGHT_CAP
              value: "125"
            - name: OPB_V2_CALIBRATION_STATUS
              value: PRODUCTION_CALIBRATED
            - name: OPB_V2_MODEL_VERSION
              value: SPXW_0DTE_OPB_AGGRESSIVE_CANDIDATE_V2
            - name: OPB_V2_DEAUTOCORR_BUCKET_MS
              value: "1000"
            - name: OPB_V2_MIN_VOL
              value: "0.03"
            - name: OPB_V2_MAX_VOL
              value: "5.00"
            - name: OPB_V2_TAU_MIN_SECONDS
              value: "30"
            - name: OPB_V2_MIN_MID
              value: "0.05"
            - name: OPB_V2_MAX_HALF_SPREAD
              value: "1.00"
            - name: OPB_V2_MAX_QUOTE_AGE_MS
              value: "3000"
            - name: OPB_V2_MAX_SURFACE_AGE_MS
              value: "30000"
            - name: OPB_V2_MAX_FORWARD_AGE_MS
              value: "30000"
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-option-price-behavior:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: option-price-behavior
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 8Gi
            requests:
              cpu: 250m
              memory: 1Gi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state

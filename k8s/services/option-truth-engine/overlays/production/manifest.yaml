# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# remaps this render via image-tags/production.yaml then pins; the monolithic
# Jenkinsfile pins from the build-resolved images env (resolve-images.sh).
# Applying this file directly bypasses that pinning and can roll out a wrong or
# stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: option-truth-engine-service
    app.kubernetes.io/name: option-truth-engine-service
    app.kubernetes.io/part-of: options-edge
  name: option-truth-engine-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8131
      targetPort: health
  selector:
    app.kubernetes.io/name: option-truth-engine-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: option-truth-engine-service
    app.kubernetes.io/name: option-truth-engine-service
    app.kubernetes.io/part-of: options-edge
  name: option-truth-engine-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: option-truth-engine-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: option-truth-engine-service
        app.kubernetes.io/name: option-truth-engine-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: HEALTH_PORT
              value: "8131"
            - name: OPTION_TRUTH_ENABLED
              value: "true"
            - name: OPTION_TRUTH_INPUT_TOPIC
              value: options.databento.gex.strike
            - name: OPTION_TRUTH_OUTPUT_TOPIC
              value: options.spx.option-truth-engine-service.by-strike
            - name: OPTION_TRUTH_STREAMS_APP_ID
              value: options-edge-option-truth-engine-service
            - name: OPTION_TRUTH_PRICING_MODEL
              value: SPOT_BLACK_SCHOLES
            - name: OPTION_TRUTH_EXCLUDE_ZERO_BID
              value: "true"
            - name: OPTION_TRUTH_EOS_ENABLED
              value: "true"
            - name: KAFKA_STREAMS_STATE_DIR
              value: /var/lib/options-edge/kafka-streams
            - name: JAVA_TOOL_OPTIONS
              value: -Xms1g -Xmx5g
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-option-truth-engine:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: option-truth-engine
          ports:
            - containerPort: 8131
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 6Gi
            requests:
              cpu: 100m
              memory: 2Gi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state

# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: databento-volume-aggregator
    app.kubernetes.io/part-of: options-edge
  name: databento-volume-aggregator
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: databento-volume-aggregator
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: databento-volume-aggregator
    app.kubernetes.io/part-of: options-edge
    oe.replay/determinism: event-time
    oe.replay/include: "true"
    oe.replay/side-effects: "false"
    oe.replay/state: ephemeral
    oe.replay/topic-remap: required
  name: databento-volume-aggregator
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-volume-aggregator
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databento-volume-aggregator
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: KAFKA_DATABENTO_EVENTS_TOPIC
              value: options.databento.events.raw
            - name: KAFKA_DATABENTO_SNAPSHOT_TOPIC
              value: options.databento.raw
            - name: KAFKA_DATABENTO_NORMALIZED_TOPIC
              value: options.databento.normalized
            - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_APP_ID
              value: options-flow-databento-volume-aggregator-v2
            - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_STREAMS_NUM_THREADS
              value: "2"
            - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_CACHE_MAX_BYTES_BUFFERING
              value: "0"
            - name: KAFKA_DATABENTO_VOLUME_AGGREGATOR_PROCESSING_GUARANTEE
              value: exactly_once_v2
            - name: JAVA_TOOL_OPTIONS
              value: -Xmx1536m
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: host.docker.internal:5001/options-edge-databento-volume-aggregator:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: databento-volume-aggregator
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 3Gi
            requests:
              cpu: 250m
              memory: 512Mi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - name: kafka-streams-state
          persistentVolumeClaim:
            claimName: databento-volume-aggregator-streams-state

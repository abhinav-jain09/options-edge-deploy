# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy path for experiment is the Jenkins experiment job
# (Jenkinsfile.experiment-deploy), which applies the monolithic overlay; there is
# NO digest pinning on this env. Applying this file by hand still violates the
# Jenkins-only deployment rule and can roll out a wrong or stale image.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8100"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: databento-gex-service
    app.kubernetes.io/part-of: options-edge
  name: databento-gex-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8100
      targetPort: health
  selector:
    app.kubernetes.io/name: databento-gex-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: databento-gex-service
    app.kubernetes.io/part-of: options-edge
  name: databento-gex-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-gex-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databento-gex-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: DATABENTO_DATASET
              valueFrom:
                configMapKeyRef:
                  key: DATABENTO_DATASET
                  name: options-edge-databento-feed-config
                  optional: true
            - name: DATABENTO_GEX_OI_TRADING_CLASS
              valueFrom:
                configMapKeyRef:
                  key: DATABENTO_TRADING_CLASS
                  name: options-edge-databento-feed-config
                  optional: true
            - name: DATABENTO_GEX_OI_HTTP_TIMEOUT_SECONDS
              value: "60"
            - name: DATABENTO_GEX_OI_FETCH_LIMIT
              value: "200000"
            - name: DATABENTO_GEX_OI_DEFINITION_FETCH_LIMIT
              value: "200000"
            - name: OI_ANCHOR_MANIFEST_ENABLED
              value: "false"
            - name: HEALTH_PORT
              value: "8100"
            - name: KAFKA_DATABENTO_RAW_TOPIC
              value: options.databento.raw
            - name: KAFKA_DATABENTO_GEX_TOPIC
              value: options.databento.gex.strike
            - name: KAFKA_GEX_STREAMS_APP_ID
              value: options-databento-gex-streams
            - name: KAFKA_GEX_STREAMS_NUM_THREADS
              value: "8"
            # gex-flow streams threads — mirrors k8s/base/databento-gex-deployment.yaml. Flow topology
            # (gex.strike -> GammaFlowProcessor -> 7 topics) defaults to 2 threads over 32 partitions;
            # each record does 3 RocksDB writes + 7-topic produce (cache off) so it blocks on I/O and
            # lags into the millions with CPU idle (prod 2026-08-01: 10.6M even across 32 parts). 8 matches
            # the main compute stage. Env-overridable (DatabentoGexBridge#flowStreamsProperties).
            - name: KAFKA_GEX_FLOW_STREAMS_NUM_THREADS
              value: "8"
            - name: DATABENTO_GEX_DEALER_SIGN_MODEL
              value: NAIVE_CALL_PLUS_PUT_MINUS
            - name: DATABENTO_GEX_OI_DIRECT_FETCH_ENABLED
              value: "true"
            - name: DATABENTO_GEX_MAX_QUOTE_AGE_MS
              value: "600000"
            - name: DATABENTO_GEX_DYNAMIC_CARRY_ENABLED
              value: "true"
            - name: DATABENTO_GEX_MARKET_CARRY_TOPIC
              value: market.spx.market-carry-service.current
            - name: DATABENTO_GEX_DYNAMIC_CARRY_MAX_AGE_MS
              value: "660000"
            - name: DATABENTO_GEX_CARRY_STATIC_FALLBACK_ENABLED
              value: "true"
            - name: JAVA_TOOL_OPTIONS
              value: -Xms1g -Xmx4g
            - name: PREOPEN_GEX_ENABLED
              value: "false"
            - name: PREOPEN_GEX_PUBLISH_ENABLED
              value: "false"
            - name: PREOPEN_GEX_SYMBOL
              value: SPX
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
            - secretRef:
                name: options-edge-databento-feed-env
                optional: true
          image: host.docker.internal:5001/options-edge-databento-gex:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: databento-gex
          ports:
            - containerPort: 8100
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 5Gi
            requests:
              cpu: 250m
              memory: 1Gi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - name: kafka-streams-state
          persistentVolumeClaim:
            claimName: databento-gex-service-streams-state

# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy paths are the Jenkins jobs, which digest-pin the image
# fail-closed BEFORE any apply: Jenkinsfile.service-deploy -> service-deploy.sh
# remaps this render via image-tags/production.yaml then pins; the monolithic
# Jenkinsfile pins from the build-resolved images env (resolve-images.sh).
# Applying this file directly bypasses that pinning and can roll out a wrong or
# stale image.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8080"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: raw-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: raw-postgres-writer
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8080
      targetPort: health
  selector:
    app.kubernetes.io/name: raw-postgres-writer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: raw-postgres-writer
    app.kubernetes.io/part-of: options-edge
  name: raw-postgres-writer
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: raw-postgres-writer
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: raw-postgres-writer
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          env:
            # Persist the feed's LIVE-captured settled OI into Postgres. The shared options-edge-config points
            # this writer at options.ibkr.raw, which does NOT carry the feed's OI, so board() (the pre-open OI
            # source, source=DATABENTO) had no live OI at 06:30. The feed publishes its live-OI capture to
            # options.databento.raw (source=DATABENTO); reading THAT here lands the settled OI in
            # databento_option_raw_snapshot so board() serves it. env overrides envFrom. (2026-08-26)
            - name: KAFKA_RAW_TOPIC
              value: options.databento.raw
          image: 192.168.100.252:5000/options-edge-raw-postgres-writer:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: raw-postgres-writer
          ports:
            - containerPort: 8080
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 256Mi

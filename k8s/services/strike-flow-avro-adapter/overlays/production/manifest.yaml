# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/production render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a PRE-PIN
# render: the image ref below is the BASE (dev-registry) ref, NOT this env's image.
# The ONLY deploy paths are the Jenkins jobs (Jenkinsfile.service-deploy ->
# scripts/deploy/service-deploy.sh, or the monolithic Jenkinsfile), which remap the
# image to image-tags/production.yaml and digest-pin it fail-closed BEFORE any apply.
# Applying this file directly to production would roll out the dev image.
apiVersion: v1
kind: Service
metadata:
  annotations:
    prometheus.io/path: /metrics
    prometheus.io/port: "8109"
    prometheus.io/scrape: "true"
  labels:
    app.kubernetes.io/name: strike-flow-avro-adapter
    app.kubernetes.io/part-of: options-edge
  name: strike-flow-avro-adapter
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8109
      targetPort: health
  selector:
    app.kubernetes.io/name: strike-flow-avro-adapter
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: strike-flow-avro-adapter
    app.kubernetes.io/part-of: options-edge
  name: strike-flow-avro-adapter
  namespace: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: strike-flow-avro-adapter
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: strike-flow-avro-adapter
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: HEALTH_PORT
              value: "8109"
            - name: KAFKA_STRIKE_FLOW_AVRO_INPUT_TOPIC
              value: options.databento.strike-flow
            - name: KAFKA_STRIKE_FLOW_AVRO_OUTPUT_TOPIC
              value: options.databento.strike-flow.strike.avro
            - name: KAFKA_STRIKE_FLOW_AVRO_STREAMS_APP_ID
              value: options-edge-strike-flow-avro-adapter-v1
            - name: JAVA_TOOL_OPTIONS
              value: -Xms256m -Xmx768m
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-strike-flow-avro-adapter:dev
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: strike-flow-avro-adapter
          ports:
            - containerPort: 8109
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            limits:
              memory: 1Gi
            requests:
              cpu: 100m
              memory: 384Mi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state

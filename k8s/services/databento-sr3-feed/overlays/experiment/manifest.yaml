# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy path for experiment is the Jenkins experiment job
# (Jenkinsfile.experiment-deploy), which applies the monolithic overlay; there is
# NO digest pinning on this env. Applying this file by hand still violates the
# Jenkins-only deployment rule and can roll out a wrong or stale image.
apiVersion: v1
kind: Service
metadata:
  name: databento-sr3-feed-service
  namespace: options-edge
spec:
  ports:
    - name: metrics
      port: 8011
      targetPort: metrics
    - name: health
      port: 8012
      targetPort: health
  selector:
    app.kubernetes.io/name: databento-sr3-feed-service
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: databento-sr3-feed-service
    app.kubernetes.io/part-of: options-edge
  name: databento-sr3-feed-service
  namespace: options-edge
spec:
  progressDeadlineSeconds: 1200
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: databento-sr3-feed-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: databento-sr3-feed-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: SR3_DATABENTO_DATASET
              value: GLBX.MDP3
            - name: SR3_DATABENTO_SCHEMA
              value: mbp-1
            - name: SR3_CONTINUOUS_SYMBOLS
              value: SR3.c.0,SR3.c.1,SR3.c.2,SR3.c.3
            - name: SR3_OUTPUT_TOPIC
              value: rates.sofr.databento-sr3-feed-service.current
            - name: HEALTH_PORT
              value: "8011"
            - name: READINESS_PORT
              value: "8012"
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-databento-feed-env
          image: 192.168.100.252:5000/options-edge-databento-sr3-feed:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 15
            periodSeconds: 15
          name: databento-sr3-feed
          ports:
            - containerPort: 8011
              name: metrics
            - containerPort: 8012
              name: health
          readinessProbe:
            failureThreshold: 30
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            limits:
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi

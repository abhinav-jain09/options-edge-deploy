# GENERATED by scripts/deploy/generate-service-slices.sh — DO NOT EDIT BY HAND.
# This is the monolithic k8s/overlays/experiment render of ONLY this service's workload
# (mirror rule holds by construction). To change the service, change the base
# manifests/overlay patches and re-run the generator.
#
# DEPLOYMENT SAFETY — NEVER kubectl-apply this manifest directly. It is a
# PRE-DIGEST render: the image ref below is at best a mutable TAG (and, where the
# overlay carries no env mapping, the BASE dev-registry ref — not this env's image).
# The ONLY deploy path for experiment is the Jenkins experiment job
# (Jenkinsfile.experiment-deploy), which applies the monolithic overlay; there is
# NO digest pinning on this env. Applying this file by hand still violates the
# Jenkins-only deployment rule and can roll out a wrong or stale image.
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: spread-skew-service
    app.kubernetes.io/name: spread-skew-service
    app.kubernetes.io/part-of: options-edge
  name: spread-skew-service
  namespace: options-edge
spec:
  ports:
    - name: health
      port: 8122
      targetPort: health
  selector:
    app.kubernetes.io/name: spread-skew-service
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: spread-skew-service
    app.kubernetes.io/name: spread-skew-service
    app.kubernetes.io/part-of: options-edge
  name: spread-skew-service
  namespace: options-edge
spec:
  replicas: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: spread-skew-service
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/component: spread-skew-service
        app.kubernetes.io/name: spread-skew-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - env:
            - name: APP_MARKET_DATA_SOURCE
              value: DATABENTO
            - name: HEALTH_PORT
              value: "8122"
            - name: SPREAD_SKEW_INPUT_SPOT_TOPIC
              value: underlying.spx.price
            - name: SPREAD_SKEW_OUTPUT_CURRENT_TOPIC
              value: options.spx.spread-skew.current
            - name: SPREAD_SKEW_OUTPUT_EVENTS_TOPIC
              value: options.spx.spread-skew.events
            - name: SPREAD_SKEW_STREAMS_APP_ID
              value: options-edge-spread-skew-service-v1
            - name: SPREAD_SKEW_STREAMS_COMMIT_INTERVAL_MS
              value: "1000"
            - name: SPREAD_SKEW_STREAMS_AUTO_OFFSET_RESET
              value: latest
            - name: SPREAD_SKEW_STREAMS_NUM_THREADS
              value: "1"
            - name: SPREAD_SKEW_EOS_ENABLED
              value: "true"
            - name: KAFKA_STREAMS_STATE_DIR
              value: /var/lib/options-edge/kafka-streams
            - name: SPREAD_SKEW_ENABLED
              value: "true"
            - name: JAVA_TOOL_OPTIONS
              value: -Xms512m -Xmx2048m
          envFrom:
            - configMapRef:
                name: options-edge-config
            - secretRef:
                name: options-edge-runtime-secrets
          image: 192.168.100.252:5000/options-edge-spread-skew:dev
          imagePullPolicy: Always
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          name: spread-skew
          ports:
            - containerPort: 8122
              name: health
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: 4Gi
            requests:
              cpu: 100m
              memory: 1Gi
          volumeMounts:
            - mountPath: /var/lib/options-edge/kafka-streams
              name: kafka-streams-state
      volumes:
        - emptyDir: {}
          name: kafka-streams-state

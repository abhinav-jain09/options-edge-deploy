apiVersion: apps/v1
kind: Deployment
metadata:
  name: raw-to-display-service
  namespace: options-edge
  labels:
    app.kubernetes.io/name: raw-to-display-service
    app.kubernetes.io/part-of: options-edge
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: raw-to-display-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: raw-to-display-service
        app.kubernetes.io/part-of: options-edge
    spec:
      containers:
        - name: raw-to-display
          image: ghcr.io/abhinav-jain09/options-edge-raw-to-display:dev
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: options-edge-config
          ports:
            - name: health
              containerPort: 8080
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 20
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 20
            periodSeconds: 10
          volumeMounts:
            - name: kafka-streams-state
              mountPath: /var/lib/options-edge/kafka-streams
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
      volumes:
        - name: kafka-streams-state
          emptyDir: {}
